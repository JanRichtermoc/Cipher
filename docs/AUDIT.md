# Cipher — security audit ledger

Every known weakness, accepted trade-off, and unfinished control, in one place.

This file exists because `Vendor/libsignal/DECISIONS.md` requires it, and because a security
argument that lives only in code comments cannot be reviewed as a whole. **An item is only
removed from here when the control that closes it exists and is tested.** Downgrading an item
to "probably fine" is not closing it.

Status legend: **OPEN** — unmitigated · **ACCEPTED** — understood, deliberate, compensated ·
**CLOSED** — a tested control exists.

**Columns.** *Adversary* names the section of [`THREAT_MODEL.md`](THREAT_MODEL.md) §1 the finding
matters to — a finding with no adversary is a bug, not a security finding. *Fix in* is the stable
step id in [`CLAUDE_IMPLEMENTATION_PLAN.md`](CLAUDE_IMPLEMENTATION_PLAN.md) that closes it, so the
ledger and the roadmap cannot drift apart.

**Ids are permanent and are cited from code, scripts and the other docs.** Never renumber a live
id; if two items ever collide, the more-cited one keeps the number and every citation of the other
is updated in the same commit.

**A CLOSED entry states the invariant and the guard that enforces it**, not the story of how it
was found. What matters going forward is what must stay true and what would notice if it stopped
being true. Compressed 2026-07-29; the narratives are recoverable from git history.

Amended 2026-08-13, second change: **5.41 is CLOSED by P9.S10 and P9.S11**, the two halves of the operator-approved
`PROPOSAL-REAUTHENTICATION.md`. P9.S10 stopped the silent destruction — a rejected rotation ran the
sign-out path, so an operator-side revoke erased each device's protocol state, history and profile up
to 23 days later; it now stops at a screen that explains, and erasure is a deliberate act. **Writing
it found a second path into the same defect**, which the first pass missed: the messaging layer's
`sessionRejected` handler also called `signOut()`, so any 401 — not only one at rotation — destroyed
local state. P9.S11 then removed the cause: an Ed25519 account key, verified with `crypto/ed25519` on
the relay and CryptoKit on the device, so neither side gains a dependency. The identity key could not
be used for this, for the reason P7.S01 already recorded — the relay cannot verify anything libsignal
signed. **The gate's High-findings box is now answerable for this row**; 6.3, 6.4 and 6.15 remain.
**The relay half is not deployed**: the staging box is on `6e6bbf2` and does not have migration
`0003`, so on that host 5.41's cost is unchanged until an operator deploys.

Amended 2026-08-13: **6.26 is opened and closed the same day** — the certificate-pinning
gate's two real-chain tests were judged against the platform trust store, so completing a path to a
publicly trusted root reached the **network**, and a stalled fetch returns a rejected chain rather than
an error. It failed twice in CI on documentation-only changes after 25 green runs, and never on the
machine it was written on, where the same evaluation completed from cache in 14 ms. Trust evaluation in
that suite is now offline, anchored, and at a fixed date. It is **R2** again in a new place: the lines
that decide the verdict had no control that could fail, so nothing said whether they were in effect.

Amended 2026-08-12: **3.8 and 3.9 are resolved to ACCEPTED by P9.S09**, with their compensating
controls named and tested. Neither is closed, and neither is downgraded to "probably fine" — the
distinction this file's legend draws. **3.8** has no available fix that does not contradict something
load-bearing: refusing an identity change contradicts locked decision §0.2.1, and checking the
certificate's *name* needs the server-issued scheme P7.S01 established cannot be built here. What is
accepted is that a §1.8 circle member can force an identity-change event against a verified contact —
**loudly**, since it clears the verified badge and blocks sending until the exact new key is accepted.
**3.9's resolution is a correction rather than a decision:** this ledger was the last document still
calling it OPEN. `THREAT_MODEL.md` §1.1's *Residual / accepted exception* row already named it, and
§3.2 already said sealing a frame is not the same as hiding the sender from a live relay — so the
ledger and the threat model disagreed about the same fact, which is the drift this file exists to
prevent. Its compensating controls are the observation-versus-record distinction the threat model
draws, and the record side is enforced: no per-delivery log line, no populated path, IPs uncorrelated
and 24h, delivered rows deleted in the acknowledging transaction, and a stored frame that names
nobody. What is accepted, without softening, is that a live relay can build the social graph as it
runs and only policy stops it recording that. It is now owned by **P10.S06** rather than unscheduled.
**The production-readiness gate stays unticked**: 5.41, 6.3, 6.4 and 6.15 remain, and P9.S09's scope
was these two rows.

Amended 2026-08-11, fourth change: **5.41 is opened OPEN by P9.S06**, and it is the fourth step in
this stretch to find its own premise wrong. The roadmap row names "mass revoke" as an incident
response; there is no such control. `DELETE /v1/auth/all` is per-account and self-service, so an
operator facing a relay compromise has no endpoint at all, and the only operator-side action is a SQL
delete whose cost is not "everyone signs in again": the session token is the **only** credential path,
so every account it touches is unrecoverable and must re-register under a different `aci`. It is also
destructive on the device on a delay — a *rotation* rejection is handled as `signOut()`, which erases
local protocol state, history and profile, and rotation begins 7 days before a 30-day expiry, so each
device erases its own history up to 23 days later with nothing linking the two events. **Not fixed
here**: a non-destructive mass revoke needs a re-authentication path that does not exist, and
inventing one inside a documentation step is what the roadmap forbids. `RUNBOOK-INCIDENT.md` documents
it as a last resort with the true cost and orders the three non-destructive containment steps ahead
of it.

Amended 2026-08-11, third change: **4.16 is opened and ACCEPTED by P9.S05**, the step that created it
rather than found it — the honest direction for a backup design to fail. Backups deliberately carry
`accounts` and `session_tokens` and nothing else, because those are the two tables whose loss no
client can repair; every other table is excluded so a restore cannot repeal `BACKEND.md` §4, and the
new gate 11 refuses a schema table that is classified as neither. Including `session_tokens` is what
opens 4.16: a restore undoes a revocation performed after the backup. It is bounded by the restored
row's own expiry, by the backup interval (RPO), and by a revoke-all step the runbook now names — and
it is explicitly **not** mitigated by encrypting the archive, since the resurrection happens after
legitimate decryption. The drill was executed on staging into a scratch database: 5 accounts and 3
tokens restored exactly, and messages, attachments, invites, one-time prekeys and push tokens all
came back **0**, which is the policy holding through a real restore rather than in a comment.

Amended 2026-08-11, second change: **no finding changed status.** The roadmap gained **P9.S09**, and
the *Fix in* cells of **3.8** and **3.9** now name it, so neither row is any longer OPEN with no owner.
Neither is closed and neither is downgraded here. Both already argue that no code change is available
or wanted — 3.8's remaining residual would need refusing an identity change, which contradicts locked
decision §0.2.1, and 3.9 needs an unauthenticated delivery path that is deliberately unscheduled and
has no abuse control behind it. That combination is precisely what makes the production gate's *High
findings closed or formally ACCEPTED with compensating controls* item unanswerable in either
direction: nobody can tick it, and nobody can point at the work that would. **P9.S09 decides; it does
not pre-decide**, and it is forbidden from closing either by argument. The same amendment relaxed P9's
precondition for the four steps that need neither the APNs key nor a production VPS; the roadmap owns
that scope.

Amended 2026-08-11: **5.40 is closed**, which empties the audit campaign's OPEN queue — both findings
it opened and could not fix are now fixed, each as its own unit. The prekey pools have a cumulative
per-account ceiling that **clips the excess instead of refusing the publication**, because the two
long-lived keys in the same request are what rotation replaces and refusing them would be a
publication lockout rather than a quota. Writing its negative tests found a defect in one of its own
new tests — it passed against the very defect it was written for — which is **R2** happening inside
this change and is recorded in the row rather than quietly fixed. Like 5.39 this is a **relay**
change, and both reached staging on 2026-08-11 — with AUDIT 6.24's compose wiring, which had never
been deployed and was found only by counting `server/` commits behind the box before touching it.

Amended 2026-08-10, third change: **5.39 is closed** — the recipient queue has a per-recipient byte
ceiling, enforced inside the enqueue statement, accepted-and-dropped so it cannot become an oracle
about the recipient. It is the campaign's first opened finding to close, and the first **relay**
change since P7.S03, so it is also the first in a while whose CLOSED status describes the repository
and not the running box — **it reached staging on 2026-08-11**, with 5.40. The cost that had to be
accepted rather than engineered away is that a refused message is silent to the sender *and* to the
recipient, which is why the ceiling is sized never to bind on honest traffic. **5.40 remains OPEN**
and was deliberately not folded in.

Amended 2026-08-10, second change: **the addressed session-establishing frame is retired.** It was
3.8's other open half — the one path where the sender's label was chosen by whoever delivered the
message and checked by nothing. Addressed `.whisper` stays, because the ratchet already makes that
label unforgeable, so a message in flight on an existing conversation is not lost. Client-only: the
relay never parses an envelope, so nothing was deployed. 3.8 stays OPEN on its remaining half, the
self-issued certificate.

Amended 2026-08-10: **3.8's residual is now pinned by a test**, which is what the campaign below
listed as its top open item and could not land. Writing it corrected the row's own mechanism —
minting a certificate that names another account is *not* enough, because the pinned libsignal binds
the sender's address into the message as well, so an impostor must claim the name in the ciphertext
too. The residual is unchanged and 3.8 stays OPEN; what changed is that it is now demonstrated
rather than argued, and the upstream binding it rests on has a test of its own. No code changed.

Last reviewed: 2026-08-09, after a **cross-cutting audit campaign** over everything already
implemented — one branch, one pull request, several focused commits, rather than one roadmap step.
It opened **2.6, 5.37, 5.38, 6.19, 6.20, 6.21, 6.22 and 6.23 and closed all eight**, opened **5.39
and 5.40 and left them OPEN** because both need a relay change and a deploy, and amended **2.4, 3.8
and 4.4** where the row was accurate about a mechanism and wrong about its scope.

The one that matters most is **2.6**: no one-time Kyber prekey was ever deleted after use.
`KyberPreKeyStore` has no `removeKyberPreKey`, so `markKyberPreKeyUsed` is the store's only
notification that one was consumed, and this store recorded the base-key witness and nothing else —
while the *curve* half was correct all along because libsignal deletes that one itself. The asymmetry
is what hid it, and two load-bearing comments, one of them a test fixture's, stated the opposite as
fact. The pool exists so a session does not fall back to the reused last-resort key; a one-time key
whose private half survives delivers none of that.

Three of the eight are gates that could not fail. **6.19**: a run with
`-skip-testing:CipherTests/CertificatePinnerTests` exits 0 and prints `** TEST SUCCEEDED **` — the
Swift Testing suites report through a different runner, no `Executed N tests` line counts them, and
certificate pinning has no XCTest anywhere. **6.20**: the relay integration floor said 104 while the
suite had grown to 140, and four whole files had no required name. **5.38**: the two nginx directives
that closed 5.16 and hold up 5.15 were enforced by a comment, in files the gate already reads.

And two are the honesty class this ledger exists for, in the direction nothing was looking. **5.37**
is not a claim of a control that is missing but a *denial* of one that is present: the
identity-change banner told the user that safety-number comparison was not implemented, in English
and in Czech, at the exact moment a key substitution produces it and one tap from the screen that
renders the digits. **6.22** is the inverse of 5.13 — the disappearing-messages disclaimer the threat
model leans on was built by concatenating two literals, which resolves to the non-localizing `Text`
initializer, so it was in no catalog and shipped in English only.

**Method and its limits.** Three independent read-only reviewers covered crypto/storage,
app/privacy/release and relay/supply-chain/operations; every finding was reproduced against the
source before any edit, and several were rejected. Every new or changed gate was negative-tested by
reintroducing the defect in the **real** file. **`verify-relay-integration.sh` was not run — Docker
was unavailable on the audit machine — so 6.20's guard was argued rather than observed.** *Resolved
2026-08-10: the CI `relay-integration` job, whose only step is `./Scripts/verify-relay-integration.sh`,
has since run that gate green on three pull requests. The condition this sentence set has been met;
see the 6.20 row.* No live host was inspected and no deployment state was changed.

Before it, 2026-08-08: **P8.S08 decided against crash reporting entirely** and enforced it
at the only place a dependency can enter the app: `verify-supply-chain.sh` now refuses a `Podfile`
declaring anything but LibSignalClient, which discharges `THREAT_MODEL.md` §4.1 and §4.4 together.
Recognising reporter SDKs by name would be a list that ages; refusing every dependency does not.
**The first version of that check was wrong in the dangerous direction, and review caught it:** it
matched only `pod 'X'`, so `pod('X')` — the same Ruby DSL method, called with parentheses — was
reported as "LibSignalClient is the only declared dependency". Reproduced against the real Podfile
before the fix and pinned by a self-test case afterwards, because a false negative here ships the
SDK. Before it, **P8.S05 re-checked the required-reason enumeration** and found it unchanged — the dependency has not moved since P1.S11 performed it, both judgement calls still hold, and the gate exits 0 against the Release build. One thing it did find: **Apple restructured the documentation**, moving the per-category API lists off the article and onto `NSPrivacyAccessedAPIType`, so a later re-check reading only the old page finds no lists and could mistake that for the requirement lapsing. Recorded in `PRIVACY_MANIFEST.md`. Before it, **P8.S04 decided there will be no notification-service extension**.
**4.4 is ACCEPTED by withdrawing its requirement rather than by meeting it** — none of its three
blockers was ever on the path to notifications, because the thing that must decrypt while the device
is locked is **wake-only push in the app's own process**, not an extension. The prohibition on adding
an NSE stands unchanged. The correction matters beyond that row: **2.1** and `THREAT_MODEL.md` §1.4
both justified the `AfterFirstUnlock` weakening by naming an NSE, so a reader arriving after this
decision would have found a locked decision with no reason left under it and could reasonably have
"tightened" it to `WhenUnlocked` — which silently breaks notifications. Both now name the real
requirement. Third time in this stretch that a step has found its own premise wrong (3.1 in P6.S02,
the sender certificate in P7.S01). Before it, **6.2 was closed** by P8.S07: libsignal's licence now ships in the
app bundle and `AboutView` renders it, discharging AGPL obligation 3. Taken out of plan order because
P8.S01 is a human step blocked on an Apple Developer account and S02/S03 depend on the key it
produces. The step's own negative test found a defect in the gate it was adding — the positive
control was the committed file, so a genuinely broken file reported that the *checker* was broken,
which is **R5** reproduced inside its own fix; the control is now synthetic. Before it, **6.18 was
closed**: a test run can no longer destroy a real
installation's keys, because `Keychain.shared` returns a Keychain on a different service while
XCTest is loaded. The redirect is in the shared handle rather than in the fixture that caused the
damage, since a fixture only protects the tests that already exist. **The obvious test for it is
deliberately not written**, and that is worth reading before someone adds it: a canary under the
production service can only be validated by breaking the redirect, and that negative control
destroys whatever real account is on the machine — a gate whose own verification is the disaster it
prevents (**R5**). It is decomposed into two properties that are each safe to prove instead.
Before it, P7.S03 hardened the push token. **No finding changed status**, and
**6.10 is worth re-reading rather than re-opening**: it recorded that §3.3's "store it hashed" was
unbuildable, and this is the step that built the amended position instead. What it found is the same
shape as **5.36** and P6.S05 — `push_tokens` was fully specified in `0001_init.sql` and `BACKEND.md`
§2.9 and **no Go had ever touched it**, so "a dump holds no plaintext token" was true only because no
token could be stored at all. It is now encrypted under a key held in the environment, refused
outright when that key is absent, swept at 30 days, and cascaded with the account — each pinned by a
test required by name. The documented cipher was unbuildable for the second time this phase: a
libsignal-free XChaCha20-Poly1305 is not in Go's standard library, so AES-256-GCM was used and
migration `0002` narrowed the nonce CHECK. **This is the phase's first change that needs a deploy.**
Before it, P7.S02 bucketed the relayed length. **No finding changed status**,
because there was never a row for message length: the leak was recorded in `THREAT_MODEL.md` §3.5 as
a decision rather than here as a defect, and it is closed on the same terms. Two things it recorded.
The roadmap said *pad the ciphertext*, and that is the one place padding cannot go — a Signal
ciphertext is authenticated, so appended bytes fail to decrypt, and making them strippable would mean
carrying the real length beside the padded one in cleartext. It goes on the plaintext. And padding is
coupled to sealing: a frame is padded exactly when it is `.sealed`, so the receive path decides by
frame type rather than by sniffing, which matters because padding bytes are valid UTF-8 and a wrong
guess would return them as content with nothing downstream noticing. Before it, P7.S01 sealed the
sender. **3.4 is CLOSED** — a relayed frame
carries seventeen zero bytes where it used to carry the sender, so a seized database can no longer
say who sent anything — and the half it did not cover was split out as **3.9** rather than closed by
omission, the same move P5.S14 made with 5.1 and 5.35: a live relay still learns the sender from the
bearer token, and closing that needs an unauthenticated delivery path nobody has approved. **3.8 is
narrowed and stays OPEN**, because the addressed receive path is still accepted for compatibility.
Two things the step found are worth keeping. The roadmap's stated mechanism — *a server-issued
sender certificate* — **is not buildable here**: those certificates are signed with XEd25519 over
Curve25519 keys, the relay is Go, and the libsignal pin ships an iOS artifact only, so issuing one
would mean writing the scheme by hand (§0.6 forbids it) or linking libsignal into the relay (a
supply-chain change needing its own approval). The certificate is therefore self-issued, and its
*name* proves nothing. Its **key** does: libsignal refuses a container whose certificate names a key
its sealer does not hold, which was discovered by a test written expecting the opposite and is now
pinned by `SealedSenderTests.testACertificateKeyTheSealerDoesNotHoldIsRefused` so an upstream change
cannot weaken it silently. Before it, P6.S05 closed the block-list and device-revoke step by finding
both halves of its own definition of done already satisfied. **No finding changed status.** Two
things it recorded. `revokeDevice` was a `MockStore` method and went with the mock in **P5.S10**
— the only remaining mention of it anywhere was the roadmap row asking for its removal, which is
worth noticing as a class: a step can be completed as a side effect and stay open on paper for
three phases. And **3.6**'s locked single-device decision is what makes "revoke observable
server-side" mean the *session token* rather than a second device; that call existed, was made on
the account-cleanup path, and was asserted by nothing until now — the same shape as **5.36**,
where a launch-time retry was dead code that read as live. Before it, P6.S04 built attachments
that are encrypted before they are uploaded. **No finding changed status, and no new one was opened.** Two things are worth recording
anyway. The relay half needed nothing: **5.22**'s byte quota and **5.23**'s delete limit were
already enforced, so the client was built against a store that already refused what it should.
And **5.21** grew a second instalment rather than reopening: the full-screen media viewer carried a
share button and a forward button with empty actions, reachable only from DEBUG fixtures until this
step made the screen show a real decrypted photo — at which point they would have shipped. They are
gone; sharing a decrypted attachment out of the app is a decision that needs a design.
Before it, P6.S03 built disappearing messages that delete rather than hide — which reverses part of **5.30**'s retirement, recorded on that row: the per-chat picker is back because the claim behind it is now true, and the two localization denylist entries that banned the words are retired with their reasoning. No finding changed status. Before it, P6.S02 resolved **3.1** as **ACCEPTED** — and corrected one of its own premises on the way: witness eviction is driven by *sending* prekey messages, not by fetching bundles, so the fetch limit P4.S06 added bounds pool drain and not this. What rotation buys is an end to the replay window rather than prevention, which is why the row is accepted rather than closed. The same review recorded **6.18**: 6.17's account-destruction guard lives in `verify-all.sh`, so a direct `xcodebuild test` bypasses it, and it already destroyed the P5.S13 simulator installation. Before it, P6.S01 closed **2.4** with prekey rotation, threshold replenishment and bounded retention of the pair each rotation replaces. The same step opened and closed **5.36**: the launch-time retry of a failed publication was dead code *and* asked for a credential phase an onboarded account never holds, so an account whose keys never reached the relay would have stayed that way — visible only as other people's 404s. **3.1 stays OPEN deliberately**: both controls it waits on now exist, and P6.S02 is where it is closed or formally accepted with its residual written down. Before it, P5.S14 closed 5.1 against named guards and split its push clause out as 5.35 (ACCEPTED) rather than closing it by omission. Before it, both findings the P5.S13 field run opened were closed: 5.34, the onboarding dead end it hit twice, and 6.17, the gate that destroyed the account it was testing against. Before it, P5.S13 delivered a real message both ways between an iPhone and the simulator, opening 5.34 and 6.17 on the way. Before it, 5.33 restored the route in the access log — a defect the same
shape as 5.32, and found while reading its logs. Before it, the first P5.S13 staging run found and
closed 5.32, the relay refusing every prekey publication a real client could send; before that,
P5.S12 closed 2.5 with a
real safety number; before that, the
staging deployment that closed 5.29 on the live box and the
backend and infrastructure remediation that closed 5.28; before them, the P5 verification and build
remediation that closed 6.14 and 6.13, the supply-chain remediation that closed 1.14 and 1.3, the
backend remediation that closed 5.27, and the transport remediation that closed 5.26.

---

## 0. Recurring failure modes

Five classes have each produced multiple findings. Stated once here as rules, because they were
first recorded as facts about one command and recurred anyway.

**R1 — Never put an infinite or long producer upstream of a consumer that exits early.**
(1.9, 1.10.) `head -c`, `grep -q` and `grep -m` all close the pipe; `xcodebuild` raises
`NSFileHandleOperationException` and GNU `tr` reports `write error: Broken pipe`, so under
`set -o pipefail` a *succeeding* pipeline fails the build. BSD and BusyBox variants die quietly,
so it works on macOS and alpine and fails on `ubuntu-24.04` — it passes locally and breaks in CI.
Buffer into a variable, or make the finite side the producer. **Live trap:** `verify-all.sh`'s
`xcodebuild | grep | sort -u | grep -q` pipelines are safe only because `sort -u` cannot emit
until it has read all input. Removing the `sort` in a cleanup reintroduces 1.9.

**R2 — A gate that has never been made to fail is not a gate.**
(1.9, 5.12, 6.7, 6.8, 5.16, 5.28, 6.25.)
Four ways it has actually gone wrong here: the check scanned prose that *describes* the control
rather than the control (comments naming the very strings being forbidden — strip comments
first); the check trusted an exit code that carries no meaning (`docker compose port` exits 0
saying `invalid IP:0`); the check aborted before running and its silence was read as an
accusation (and again in 6.25, where `set -e` aborted the script *before* the guard written
for that very case, so the rule this file already states was skipped rather than absent); and
the check was run with an instrument that cannot express the negative (macOS
`openssl` is LibreSSL and cannot drive a `-tls1_3` probe, so it reported an impossible
"1.2 refused / 1.3 refused"). Every gate in this repo is negative-tested by reintroducing the
defect. A gate that cries wolf is as dangerous as one that never fires — it gets deleted.

**R3 — `#if DEBUG` fences code, not resources.** (5.10, 5.11.) `Localizable.xcstrings` compiles
into the bundle regardless, and Xcode emits every translated `stringUnit` even for keys it drops
from `en.lproj`. Any audit of "what ships" must read the whole `.app`, not the Mach-O.

**R4 — A control removed from the source survives in its translations.** (5.4, 5.10, 5.13, 5.14.)
Deleting an English string leaves its `cs` rendering in the catalog, and a guard list written in
English silently exempts every other language ("passcode" → "kód zařízení").
`Scripts/verify-localization.py` searches every language and carries a `--self-test` that
reintroduces each defect before its own verdict is believed.

**R5 — A self-test can be wrong in the same way the check is.** (6.14, 6.16, 5.29, 1.14, 6.25.)
Every gate here carries a self-test, and writing them surfaced four distinct ways the
self-test itself passes without proving anything. **Do not derive the cases from the data
under test:** the simulator-fault self-test looped over the pattern list, so deleting a
pattern deleted its own case and the suite stayed green — the exact defect 6.14 records,
reproduced inside its fix. Observed inputs go in as literals. **`set -e` turns a bare
call into a silent exit:** `chain_is_verified "$x"` on its own line aborts the script when
it returns non-zero, which made a pin self-test exit 1 with no output *and* would have
turned "this certificate failed validation" into a silent failure at the live call site.
Use `|| rc=$?`. **A subshell cannot report a failure flag:** running checks inside
`$(...)` to capture their output left `FAILED` in the child, so five negative cases read
as accepted. Redirect to a file instead. **And a wait is not free of the harness:** test
ceilings must stay below whatever escape hatch the fixture uses, or a wait that outlives
it stops observing the property and starts observing the timeout — silently a different
test (6.16).

A self-test that has never been made to fail is worth exactly as much as the gate it is
supposed to validate. Reintroduce the defect in the *real* file, not only in a fixture:
that is what caught the two cases above where the fixture was fine and the wiring was not.

---

## 1. Supply chain

Adversary §1.7 throughout: a compromised dependency runs our code on every device, inside the
trust boundary, where encryption does not help.

| # | Item | Status | Adversary | Fix in |
|---|---|---|---|---|
| 1.1 | **The iOS libsignal FFI binary is unsigned and carries no provenance attestation.** A SHA-256 pins bytes, not origin: a malicious build served by `build-artifacts.signal.org` with a published hash would be reproduced faithfully. | ACCEPTED | §1.7 | — |
| 1.2 | **`bin/fetch_archive.py` enforces its checksum with a bare `assert`,** which `python -O` strips. `Scripts/verify-supply-chain.sh` therefore verifies independently rather than trusting the pod's own check. | ACCEPTED | §1.7 | — |
| 1.3 | **`Podfile.lock` recorded a git *tag*, which is mutable upstream. Closed 2026-08-06 with 1.14.** The lockfile now records `:commit: 97801d22…` because the Podfile resolves the commit rather than the tag, so the build no longer depends on what `v0.99.1` points at. `PINS.env` remains the single source of that commit, and `Scripts/verify-supply-chain.sh` asserts the lockfile matches it and still re-resolves the tag — the latter now as an upstream-change alarm. Enforced on every push and PR by CI (1.6), and negative-tested by the gate's own self-test. | CLOSED | §1.7 | P5 supply-chain remediation |
| 1.4 | **libsignal is deliberately `0.x`** and promises no stability between releases. `LibsignalContractTests` pins the behaviour this codebase depends on, so a bump fails there rather than in production. | ACCEPTED | §1.7 | — |
| 1.5 | **`ENABLE_USER_SCRIPT_SANDBOXING = NO`** — CocoaPods' script phases cannot run sandboxed. A real reduction in build-time isolation, compensated by 1.2 and 1.3. **Narrowed 2026-07-30:** it was applied to the whole project and every target; it is now off only for the three targets that **embed** the pod framework, because `[CP] Embed Pods Frameworks` shells out to `rsync` and sandboxed that fails with `deny(1) file-write-unlink` on `LibSignalClient.framework/Info.plist`. Measured target by target, not reasoned about. `CipherCrypto` links without embedding, so it is sandboxed — which is the target that matters most, since the identity key and the record store live there — and the **project default is now `YES`**, so a target added later starts sandboxed. The setting is per target and not per phase, so no narrower exemption exists. | ACCEPTED | §1.7 | — |
| 1.6 | **CI runs the full gate on every push to `main` and every PR.** Xcode is selected from `PINS.env`'s `VERIFIED_XCODE` — never a hard-coded path, because runner images are versioned (`Xcode_26.6.app`). **Required status check enabled 2026-07-30** by the operator, as a repository ruleset named `main-protection` targeting the default branch, requiring `verify` and `relay-integration`. `release-gate` is deliberately **not** required: it is gated on `if: startsWith(github.ref, 'refs/tags/')`, so requiring it would leave every pull request waiting on a check that never starts. Nothing in this tree can assert a repository setting, so the guard here is the setting itself plus this row naming what it must contain. | CLOSED | §1.7 | P1.S09, P1.S10 |
| 1.7 | **The whole tree is under version control, `Pods/` included** — which is what makes `.gitignore:23`'s stated supply-chain review control ("so the libsignal Swift wrapper is diffable on every dependency bump") real rather than described. Do not start ignoring `Pods/`. | CLOSED | §1.7 | P1.S04 |
| 1.8 | **CI actions are pinned to full commit SHAs**, not tags — the same weakness as 1.3 but with a larger blast radius, because that code runs in CI holding a token that can write to this repository. `.github/dependabot.yml` updates them by reviewed PR. **CocoaPods and libsignal are deliberately excluded from dependabot:** an automated bump would change a cryptographic dependency's commit and checksum in one PR and bypass the review the supply-chain gate exists to force. | CLOSED | §1.7 | P1.S09 |
| 1.9 | **A CI step failed the build by verifying successfully** — SIGPIPE against `xcodebuild`. See **R1**; three sites fixed by buffering output before parsing. | CLOSED | §1.7 | P1.S09 |
| 1.10 | **SIGPIPE broke CI again in a different tool** after 1.9 had documented the mechanism, because the lesson had been recorded about one command instead of as a rule. Password generation is now `head -c 32 /dev/urandom \| od -An -tx1 \| tr -d ' \n'`. **Hex, not base64:** the value is interpolated into `postgres://user:PASSWORD@host`, where `/`, `+` or `=` corrupt the URL. See **R1**. | CLOSED | §1.7 | P4.S03 |
| 1.11 | **A dependency with a published, reachable CVE sat in `server/go.mod` for two phases, and nothing in this repository would ever have said so.** `golang.org/x/text` < v0.39.0 infinite-loops in `norm.Iter` on invalid UTF-8 (CVE-2026-56852), reachable from `store.Open` → `pgxpool.NewWithConfig` → `norm.Form.Transform`. **The gap was structural, not an oversight:** `verify-supply-chain.sh` checks that dependencies are the ones we *pinned*, which is a different question from whether what we pinned is safe, and `dependabot.yml` deliberately watches GitHub Actions only (1.8). Bumped to v0.39.0 — the minimum fixed version, not the newest, so the diff is the fix and nothing else; it carries `golang.org/x/sync` v0.21.0 with it because v0.39.0 requires it. `Scripts/verify-vulns.sh` now runs `govulncheck` as gate 5 of `verify-all.sh`, pinned to a version, refusing to let the scan modify `go.mod`, and skipped only by `--offline` (where the release job re-runs it). Negative-tested by reverting the module and confirming the gate exits non-zero. Reachability-based rather than version-based, so it does not report a vulnerability in vendored code we never call — the difference between a gate someone acts on and a list someone ignores. | CLOSED | §1.7 | Security audit remediation, 2026-07-30 |
| 1.13 | **The vulnerability gate scanned a different standard library than the one that ships, so it passed locally and failed in CI — on a finding it was added to catch.** `verify-vulns.sh` (1.11) ran `govulncheck` against whatever Go was on `PATH`. Most of what it finds is in the **standard library**, which comes from the toolchain and not from the module graph, so the verdict was a property of the developer's machine: the same tree reported *No vulnerabilities found* under Go 1.26.5 and **21 reachable vulnerabilities** in CI, which installs the version `server/go.mod` declares (`go-version-file`). Those 21 were real — `crypto/tls` handshake and session-resumption defects, three `crypto/x509` chain-building paths, two `net/url` parsing bugs, all reachable from `relay.run` → `http.Server.ListenAndServe` and from `store.Open` → `pgxpool.ParseConfig`, which is the public-facing TLS surface of a host the threat model already assumes is attacked. **The gate was green on the one machine where the fix would be written** — R2 in its purest form, an instrument that could not express the negative. Two things are fixed. `server/go.mod` declares **`go 1.25.12`**, the lowest release fixing all 21 and deliberately not the newest, so the diff is the fix and nothing else (1.11's rule); 1.25.0 remains the measured *language* floor beneath it, because pgx v5.10.0 requires it. And the gate now **pins `GOTOOLCHAIN` to the version go.mod declares**, so a local run is evidence about the release build; a two-part version (`go 1.25`) names no released toolchain and is refused loudly rather than silently falling back to the `PATH` Go, since falling back is the defect itself. Measured, not argued: clean under 1.25.12, 21 findings under 1.25.0. Negative-tested twice — reverting the directive to 1.25.0 fails the gate and the message names the stdlib case, and a partial version fails with the refusal above. The failure text also grew case 1: it previously offered only `go get <module>@<version>`, which cannot fix a stdlib finding, so the CI output pointed nowhere useful. **Stated residual:** `verify-relay.sh` still builds, vets and tests under the `PATH` toolchain rather than the declared one. Milder by construction — CI runs that gate under the declared version, so a divergence surfaces there rather than becoming a false security all-clear — and 1.25.12 was verified by hand here (build, vet, `go test -count=1`, all clean). | CLOSED | §1.7 | CI fix, 2026-07-30 |
| 1.14 | **CI resolved a mutable libsignal tag before anything proved which commit it named. Closed 2026-08-06 (P5 supply-chain remediation).** `pod install` checks out the dependency and runs its podspec — including the script phase that fetches the FFI archive — and `verify-supply-chain.sh` compared the tag to `PINS.env` only afterwards, inside `verify-all.sh`. So a moved tag bought build-time execution on a runner holding a repository token, and the gate could only report it once that had already happened. Ordering was the vulnerability, not the comparison. **The fix removes the resolution rather than moving the check earlier:** the Podfile declares `commit: PINS['LIBSIGNAL_COMMIT']`, so CocoaPods checks out the audited bytes and never asks the remote what `v0.99.1` points at today; `Podfile.lock` records `:commit:` in both `EXTERNAL SOURCES` and `CHECKOUT OPTIONS`, and the pod's `SPEC CHECKSUMS` entry was unchanged by the switch, which is the evidence that the tag did still name the reviewed commit. `--pre-install` keeps it true: an offline mode of the same gate, run immediately before `pod install`, refusing if the Podfile has been loosened back to a tag, if it pins a commit from outside `PINS.env`, or if the lockfile records anything else. It also now runs under `--offline`, where the whole gate used to be skipped. The tag→commit check survives with its role restated: an **alarm that upstream changed**, no longer the control standing between a moved tag and execution here. And the workflow declares `permissions: contents: read` at the top level — previously the token's scopes came from a repository setting that is not visible in the file, not under review, and changeable without a commit. Guards: `verify-supply-chain.sh --self-test` (8 cases) runs as part of gate 1 and reintroduces every defect — a tag in the Podfile, a tag in the lockfile, an unaudited commit, a returned branch snapshot — and includes the **R3** control that a comment naming `tag:` must not fire the check, because the Podfile now carries a paragraph explaining the rule in exactly those words. **Two residuals.** (a) The check is a grep over the whole Podfile, exact while LibSignalClient is the only dependency; a second pod would make it coarse, and its failure direction is refusal. (b) Nothing here attests *provenance* — 1.1 is unchanged, and a pinned commit is still bytes without a signature. | CLOSED | §1.7 | P5 supply-chain remediation |
| 1.12 | **The committed vendor tree had drifted from upstream, and nothing could have noticed.** `go mod verify` checks the module *cache* against `go.sum`; a `-mod=vendor` build then reads `server/vendor/` and compares it to nothing at all — so the claim that "every dependency byte is in the diff" (1.7's argument, applied to Go) was true about the diff and not about the bytes. Found while re-vendoring for 1.11: `github.com/google/uuid`'s copy had had its doc comments reformatted, most likely by a `gofmt -w .` over the whole tree, since this repository's format gate deliberately excludes `vendor/`. Comments only this once. `Scripts/verify-relay.sh` now re-vendors into a temporary directory and requires a byte-identical tree. `.DS_Store` is the only exclusion — untracked, and Finder recreates it in any directory a developer opens, so failing on it would make the gate cry wolf on every macOS machine (**R2**). Negative-tested by appending a comment to a vendored file. | CLOSED | §1.7 | Security audit remediation, 2026-07-30 |

## 2. Key custody

| # | Item | Status | Adversary | Fix in |
|---|---|---|---|---|
| 2.1 | **Keychain accessibility is `AfterFirstUnlockThisDeviceOnly`, not `WhenUnlockedThisDeviceOnly`** (locked decision). After first unlock the identity key is reachable by any code executing on the device. `WhenUnlocked` was rejected because something must decrypt while the device is locked, and server-visible notification content is a far larger leak. `ThisDeviceOnly` is the non-negotiable half, asserted by `testStoredItemsAreDeviceOnlyAndAvailableAfterFirstUnlock`. **Premise corrected 2026-08-08 (P8.S04): the something is not an NSE.** This row used to say `AfterFirstUnlock` was needed "so an NSE can decrypt while locked", and P8.S04 decided there will be no NSE (4.4) — which would have read as removing this decision's only justification, and invited a tightening to `WhenUnlocked` that breaks notifications. The real requirement is **wake-only push**: a silent push wakes the *app*, which fetches, decrypts and posts a local notification, with the device still locked and in the app's own process. The conclusion is unchanged and the reason is now the true one. Do not "tighten" without a notification-content redesign. | ACCEPTED | §1.4 | — |
| 2.2 | **libsignal's own buffers are never zeroized.** `serialize()` wraps a Rust allocation released by a plain `drop`. `SecretData` wipes our copy only; the Rust-side buffer is outside our control. Measured in `ZeroizationRealityTests`. | ACCEPTED | §1.4 | — |
| 2.3 | **Swift cannot promise a value is never copied.** Anything derived from a secret (`Data`, `[UInt8]`, `String`) is a copy `SecretData` can never reach. The type's guarantees are deliberately narrow. | ACCEPTED | §1.4 | — |
| 2.4 | **Prekeys are rotated and the pool is replenished. Closed 2026-08-07 (P6.S01).** Invariants. **(a) Publishing is rotating.** The relay refuses an upload without a signed prekey and a last-resort Kyber key (`BACKEND.md` §2.5/§2.6), so there is no publish-without-rotate shape to build: every publication mints both, and the relay replaces its stored pair. That is what bounds a compromised prekey — a leaked private half is useful for starting sessions only until the relay stops serving its public half. **(b) The pair that is replaced is retired, not deleted.** A peer that fetched the bundle a moment earlier names the *old* signed prekey in their first message, and deleting the private half on the spot makes that message permanently undecryptable for someone who did nothing wrong. `CryptoEngine.retiredPreKeyRetentionMs` is thirty days; the rotation record names each retired id and `recordPreKeyRotation` prunes them once it has elapsed, so retention is bounded rather than a leak, and a clock that moved backwards keeps a record rather than dropping it. **(c) The scheduler runs from the relay's count and the local one, whichever is lower.** The relay reports this account's own remaining pools when it accepts a publication (never anyone else's — §2.4 of the backend doc). That count is the pool a drain empties and is stale by construction; the local count falls only when a peer actually sends and is the half a hostile relay cannot lie about. Below `replenishThreshold` the pool is topped up **by the shortfall**, because the relay adds one-time keys rather than replacing them and republishing the target would grow both pools without bound. **(d) A publication is recorded only after the relay accepted it**, so a failed rotation is retried rather than looking complete. **(e) A `.destroying` credential cannot publish** — keys must not outlive the account they belong to. Guards: seven `PreKeyRotationTests` and seven `MessageRepositoryTests` named in the P6.S01 commit. Negative-tested by reintroducing six defects in the real source — deleting the superseded pair, never pruning, dropping the publication floor, restoring the `.registering`-only credential, minting the target instead of the shortfall, and recording the publication before the relay accepted it — each failing its named test and no other. **Three residuals, stated rather than implied.** (a) An installation that published *before* this record existed holds one signed prekey and one last-resort Kyber key whose ids nothing recorded; the record store deliberately offers no enumeration, so those two survive until the account is erased. (b) A pool drained to zero between two checks is not observable — nothing tells the device a bundle was dispensed — so the rotation interval is also the ceiling on how long that lasts. That residual belongs to 3.1 and closes with it in P6.S02. (c) Base-key witness records for retired prekeys are not pruned with them; they are bounded per pair and carry no key material, and pruning them needs a pairing this record does not track.  **Fourth residual, recorded 2026-08-09 (audit campaign):** consumption tombstones are never pruned. `RecordStore.remove` writes a sealed tombstone per removed record and only `removeAll` clears them, so each consumed one-time prekey leaves a permanent row — and `count(_:)` unseals both namespaces, so `remainingPreKeyCount()` costs one AEAD open per prekey ever consumed. Bounded in practice by the P4.S06 fetch limit (10/hour, 30/day per caller) and outside 4.14's conversation quota, but the bytes count against the same logical ceiling. Not fixed: the cheap path is a row count that does not unseal, and that is a change to the storage layer rather than to this row's control. | CLOSED | §1.2 | P6.S01 |
| 2.5 | **No user-facing safety-number screen. Closed 2026-08-06 (P5.S12).** Trust state existed, persisted, survived restart and blocked sending — but nothing rendered it, so 3.2's protection was half-delivered: a user was stopped from sending with no way to see *which* key they were being asked to accept, which is the difference between a key rotation and an attack. `CryptoEngine.safetyNumber` derives the sixty digits through libsignal's `NumericFingerprintGenerator` over both identity keys and both ACIs; iterations and version are format constants, not settings, because two builds disagreeing on either would produce different numbers for the same pair and users would read that as an attack. **Verification is stored bound to the exact key** — a `verifiedFlag` bit inside `PeerIdentityRecord` rather than a separate table — so there is no record of "this peer is verified" that could outlive the key it was asserted about, and `saveIdentity` writes a changed key with the bit clear. That makes "invalidate on identity change" a property of the format rather than of someone remembering to clear it. **Accepting and verifying are kept separate**, which is 5.4's lesson applied: accepting a changed key unblocks sending, verifying asserts the digits were compared with the person, and a single control for both would make the badge mean only that a warning was dismissed. The screen says so in its own copy. Both write paths name the key they apply to, so a change arriving between the screen being drawn and the tap is refused rather than silently approved. The record format anticipated this exactly: its `knownFlags` comment already named "a future build — one that had, say, added a *verified* bit" as the case it refuses rather than misreads, so a build predating this reads a verified peer as unreadable instead of unverified. That direction is deliberate — a discarded verification looks conservative and is not, because it silently retracts a claim the user made after comparing digits. The cost is a downgrade path that loses peer identity records, bounded by `saveIdentity` treating an unreadable record as first contact. `VerificationDisplay.isAvailable` flips to true and the badge is backed by a real claim for the first time; the gate itself stays, so turning it off again remains one edit. The localization denylist entry forbidding "Safety Number" is retired with its reasoning, because the claim it existed to stop is now true — and what replaces it is `SafetyNumberTests`, since a denylist can only stop a retired claim returning, never a new invention (5.4's recorded residual). Guards: nine `SafetyNumberTests`, including the plan's two "Done when" halves proved against **two real engines** rather than an engine and a fixture — `testTwoEnginesAgreeOnTheSafetyNumber` and `testASubstitutedIdentityChangesTheSafetyNumber`. Negative-tested by reintroducing each defect in the real source: carrying the verified bit across a key change failed `testVerificationIsClearedWhenTheIdentityChanges` by name with "a verified badge survived an identity change"; dropping the exact-key check failed `testVerifyingRefusesAStaleKey`; and making acceptance confer verification failed `testAcceptingAChangedKeyDoesNotVerifyIt`. **Two residuals, stated rather than implied.** (a) QR scanning is deliberately not built: it needs a camera permission this app has never had and a new attacker-facing input path, and a QR *render* with no scanner would be an inert affordance of exactly the kind 5.4, 5.21 and 5.30 removed. Scoped out with the operator, not dropped. (b) The digits protect the key, not the label: AUDIT 3.8's first-contact relabelling is unchanged, because comparing a number confirms which key is in use and not which name the relay attached to it. | CLOSED | §1.2 | P5.S12 |

| 2.6 | **A used one-time Kyber prekey kept its private half forever. Found and closed 2026-08-09 (audit campaign).** `KyberPreKeyStore` has no `removeKyberPreKey`: `markKyberPreKeyUsed` is the only notification a store gets that a Kyber prekey was consumed, so deleting is the store's job, and this store only recorded the base-key witness. Nothing else could have done it — the sole `.kyberPreKey` removal in the tree was `recordPreKeyRotation`'s prune, which only ever names a **last-resort** id. So every one-time Kyber private key ever dispensed was still on disk. **What that costs.** The one-time pool exists so a session does not fall back to the reused last-resort key whose KEM contribution is shared with every other session that also fell back (`BACKEND.md` §2.6). A one-time key that is never deleted delivers none of that: against an adversary who records a session-establishing message and later reads the device (§1.1 or §1.3 with §1.4), a quantum-capable attacker recovers every X25519 contribution in PQXDH from public values, leaving the KEM secret as the only barrier — and a retained private key is not a barrier. Recovering the initial root key yields the chain below it. The curve half was always correct: libsignal calls `removePreKey` itself, which is the asymmetry that hid this. **Two load-bearing comments asserted the opposite** — `markKyberPreKeyUsed`'s own doc said the replay risk was against the last-resort key "(which is not consumed on use)", and `PreKeyRotationTests` stated as fact that "a one-time Kyber prekey is deleted when it is used". Both are corrected rather than deleted, because the false premise is the reason the gap survived review. **Invariant.** After a session is established against a one-time Kyber prekey, `loadKyberPreKey` for that id throws. The last-resort key is never deleted, live or retired, and every uncertainty — no rotation record, an unreadable one — resolves to *keep*: deleting a key still being handed out costs a peer their first message permanently, keeping one costs only this. **Guards:** `PreKeyRotationTests.testAUsedOneTimeKyberPreKeyIsDeleted` (with the curve half as its positive control — if that key survived, no session ran), `testTheLastResortKyberPreKeySurvivesBeingUsed`, `testARetiredLastResortKyberPreKeySurvivesBeingUsed`, and `testARepeatedFirstMessageStillDecryptsAfterTheKyberPreKeyIsDeleted`, which pins the property the deletion depends on: libsignal returns early from `process_prekey` for a base key it already has a session state for, so a resent first message never consults the store again. Negative-tested by disabling the deletion in the real source: `testAUsedOneTimeKyberPreKeyIsDeleted` failed with "a used one-time Kyber prekey must not survive its use (AUDIT 2.6)" and no last-resort test failed. **Corrected the same day, by adversarial review, before this branch was proposed.** The first version of the deletion claimed the last-resort key is never deleted and did not achieve it: `recordPreKeyRotation` appends the outgoing pair to `retired` only when there *was* a previous record, so the **first** rotation of an installation that published before the record existed writes one naming neither legacy id — and that installation's legacy last-resort key then looked exactly like a one-time key and was deleted on first use. That is the permanent `invalidKeyIdentifier` for a peer holding the old bundle that `recordPreKeyRotation` retains keys to prevent, and this project has a device in that state (plan STATUS, P5.S13). The rule is now bounded by the monotonic counter: `generatePublishedKeys` reserves the signed prekey first, so an id below the live `signedPreKeyId` is one this publication did not mint and is kept. Pinned by `testALastResortKeyMintedBeforeTheRotationRecordSurvivesTheFirstRotation`, which reaches the 2.4(a) state deliberately. **Residual, and it is a real cost:** a one-time Kyber key from a *superseded* publication is also kept, because nothing recorded at mint time tells it apart from a legacy last-resort key. The relay serves older pools too (5.40), so this is not empty. Closing it needs the kind recorded when the key is minted — a record-format change, and its own step. **Secondary effect, and it is a trade rather than a saving.** An ML-KEM private key stops accumulating one per session ever established — which nothing observed, because `remainingPreKeyCount()` counts the curve pool only — but `RecordStore.remove` writes a tombstone, so a near-empty sealed row takes its place, and that is 2.4's fourth residual. The base-key witness for a deleted key is **not** written at all (added 2026-08-09 after review): libsignal loads the Kyber prekey before it calls `markKyberPreKeyUsed`, so once the key is gone the pair is unreachable and the row could never fire. The last-resort key still gets one, which is the case the witness was written for. Pinned by `testADeletedOneTimeKeyLeavesNoWitnessAndTheLastResortKeyStillDoes`, and negative-tested by restoring the unconditional write, which failed it with "a deleted key's witness could never fire, so it must not be written". The two fail-safe branches — no rotation record, an unreadable one — are now covered too, by `testAKyberPreKeyIsKeptWhenThereIsNoRotationRecord` and `testAKyberPreKeyIsKeptWhenTheRotationRecordIsUnreadable`. | CLOSED | §1.1, §1.3, §1.4 | audit campaign 2026-08-09 |

## 3. Protocol

| # | Item | Status | Adversary | Fix in |
|---|---|---|---|---|
| 3.1 | **The base-key witness is a bounded FIFO (512 entries per prekey pair) and evicts rather than fails.** Anyone holding the published bundle can start a session, so it has an unauthenticated growth vector; unbounded is disk exhaustion, failing closed lets an unauthenticated party permanently disable session establishment. Eviction degrades instead. **Half done (P4.S05/S06):** prekey fetches are rate-limited 10/hour and 30/day **per caller, not per target** — keying by target would let several attacker accounts drain one pool at N times the rate while every bucket looked healthy, and would let anyone deny service to a peer. Measured: without the limit 60 fetches consumed 60 prekeys; with it, 10. **Resolved as ACCEPTED 2026-08-07 (P6.S02), with one of its own premises corrected.**

**The correction, because it was load-bearing and wrong.** This row and `THREAT_MODEL.md` §1.2 both said witness eviction is driven *by fetching prekey bundles*, and credited the P4.S06 fetch limit as its mitigation. It is not, and the limit does not bound it. Filling the witness needs 512 distinct **base keys** against one (kyber prekey, signed prekey) pair, and a base key costs an attacker nothing: one fetch yields a bundle, and `processPreKeyBundle` can be run against that same bundle any number of times, each run minting a fresh base key. Nor must the attacker spend the one-time pool — they assemble the `PreKeyBundle` themselves from the public halves and may name the **last-resort** Kyber key directly, or omit the one-time prekey entirely. So the cost is 512 sends, and `POST /v1/messages` allows 60 a minute per account: about nine minutes, from a single authenticated account, with the fetch budget barely touched. That figure is arithmetic over the published limit and the code path, not a measurement. Two attacks were being conflated. **Fetch rate limiting bounds pool drain** — a denial of service against session setup, real and separately worth having. **It does not bound witness eviction**, which is a send-side cost.

**What rotation does buy, and it is the reason this is acceptable rather than merely known.** Before P6.S01 a captured prekey message stayed replayable for the life of the installation, because the last-resort Kyber key and the signed prekey it named were never replaced. Now both are replaced every 48 hours and the superseded pair is pruned thirty days later, after which session setup against it fails on the missing key — regardless of what the witness does or does not remember. The replay window is bounded by construction instead of by an attacker's patience.

**The residual, stated plainly.** Inside that window an attacker who already holds an invite-issued account can flush the witness for a live pair in minutes and then re-establish a session against the reusable last-resort key, which re-delivers one captured ciphertext and displaces the recipient's current session with that peer. It buys **no plaintext and no key material**: the attacker cannot read the message they are replaying, and identity keys, the ratchet and the signed-prekey signature are untouched. The reachable harm is a duplicate delivery and a session displacement, from an adversary who must already be inside a closed circle that only grows by invitation (3 invites/day/account).

**Why ACCEPTED and not CLOSED.** This ledger's rule is that CLOSED means a tested control exists, and no control here prevents witness eviction — the FIFO still evicts rather than fails, which remains the deliberate choice between disk exhaustion by an unauthenticated party and letting one permanently disable session establishment. Marking it CLOSED on the strength of rotation would be exactly the "downgrade to probably fine" this file opens by forbidding.

Guards: `ProtocolStoreTests.testReplayedBaseKeyIsRejected`, `testBaseKeyWitnessSurvivesAStoreRestart` and `testBaseKeyWitnessEvictsOldestAtCapacity` pin the FIFO and its eviction; `PreKeyRotationTests.testPruningTheLastResortKeyEndsSessionSetupAgainstTheSupersededPair` pins the compensation this acceptance rests on, with a live pair and a retired-but-retained pair as its positive controls and a distinct unconsumed one-time prekey per attempt so neither the pool nor the witness can explain the refusal. Negative-tested by disabling the prune in the real source, which fails that test by name. **Pool-drain residual, unchanged** (BACKEND.md §5): exhausting the X25519 one-time pool denies session setup with that peer until they replenish, since `PeerKeyBundle` requires a one-time prekey and that pool has no last-resort fallback. Existing sessions are unaffected. Replenishment bounds how long that lasts rather than preventing it — a dispense is invisible to the owner, so a drain is noticed at the next publication and the rotation interval is the ceiling. | ACCEPTED | §1.2 | P4.S06, P6.S01, P6.S02 |
| 3.2 | **A changed peer identity is trusted for receiving** (locked decision). Refusing would drop messages silently and train users to ignore the warning. **Sending is refused until `acceptIdentity` names the exact key the user was shown.** Both directions tested. | ACCEPTED | §1.2 | — |
| 3.3 | **Trust on first use.** A first sighting has nothing to compare against. Safety numbers are the out-of-band check and are blocked on 2.5. | ACCEPTED | §1.2 | — |
| 3.4 | **The relayed frame no longer names its sender.** Every outbound envelope is `.sealed` (P7.S01, 2026-08-08): the Signal ciphertext is wrapped in libsignal's sealed-sender container, addressed to the recipient's identity key, and the wire's seventeen sender bytes are zero — checked on decode, not merely unwritten, so a client cannot fill them in by accident and a relay cannot fill them in on purpose. **Invariant:** nothing a seized relay database holds identifies who sent a message. Guards: `SealedSenderTests.testTheRelayedFrameNamesNobody` searches the real send's bytes for both the sender's wire encoding and its raw UUID, with an addressed frame as the positive control that the search can find a sender when one is there; `MessageRepositoryTests.testASentMessageIsCiphertextTheRelayCannotRead` repeats it end to end through the repository; `EnvelopeTests.testSealedEnvelopeCarryingASenderIsRefused` pins the zero rule. Negative-tested by restoring the addressed send in the real source (*"preKey is not equal to sealed"*) and by dropping the zero check (*"did not throw an error"*). **What this row does not cover is now 3.9**: the relay is still told who is sending by the bearer token on `POST /v1/messages`, so this closes the *record* a seized host holds and not the *observation* a live one can make — which is the distinction this row was written on ("a record, not an inference"). | CLOSED | §1.1 | P7.S01 |
| 3.5 | **`PlaintextContent` / `DecryptionErrorMessage` is refused at the wire boundary** (locked decision). Accepting it hands a malicious relay a free session-reset primitive. Session resets must travel as plaintext *inside* an ordinary encrypted message. See `Envelope.PayloadType`. | CLOSED | §1.2 | — |
| 3.6 | **Single device only** (locked decision). `deviceId` is absent from the wire format; multi-device requires `wireVersion` 2. Do not sneak it in. | ACCEPTED | — | P10.S01 |
| 3.7 | **No group messaging** (locked decision). `SenderKeyStore` is deliberately unimplemented and `.senderKey` is rejected by `Envelope.payloadType(for:)`, so no untested sender-key state is reachable. | ACCEPTED | — | P10.S02 |
| 3.8 | **A session-establishing message can be relabelled by the relay.** Attribution follows the session that authenticated the ciphertext, never `Envelope.sender` — for an established session a strong guarantee (`testRewrittenEnvelopeSenderCannotMisattribute`: a rewritten field yields a dropped message, not a misattributed one). A **first** message is weaker: the address is chosen by whoever relayed it and the identity key comes from inside the message, so a hostile relay can present a genuine first message from A under an address the user knows as B. The content is unforgeable; the label is not. `DecryptedMessage` therefore surfaces `senderIdentityKey` alongside `sender`, and `establishedSession` marks which messages are held to the weaker standard. **Narrowed but not closed by P7.S01 (2026-08-08).** On a sealed frame the label is *inside* the ciphertext, so a relay can neither read nor rewrite it, and re-wrapping a payload it captured under a name of its own is refused: libsignal binds the certificate's key to whoever sealed the container, and the receive path then requires that key to be the key the session authenticated (`SealedSenderTests.testAThirdPartyCannotResealAnotherAccountsCiphertext`, negative-tested by removing the check). **Two things kept it open, and one is now gone (2026-08-10).** The addressed receive path carried the old weakness unchanged; the **session-establishing** half of it is now refused (`EnvelopeError.addressedFirstMessageRefused`, `SealedSenderTests.testAnAddressedFirstMessageIsRefused`, negative-tested by restoring acceptance). Addressed `.whisper` is deliberately kept: on an established session the ratchet MAC verifies only under that session's keys, so a rewritten sender yields a dropped message rather than a misattributed one, and keeping it means a message in flight on an existing conversation is not lost. **The cost is stated rather than hidden**: a *first* message sent by a build older than P7.S01 and still pending on the relay is now refused, and the receive path treats that as permanent and acknowledges it away. That was affordable because no client has produced one since P7.S01 and the relay never parses the envelope (`server/internal/api/messages.go`), so this is client-only and needed no deploy. And the certificate is self-issued (3.4), so it constrains a *relay* and not a *sender*: an account can still name itself anything, which is what safety numbers are for.  **Amended 2026-08-09 (audit campaign): the relay is not the only party who can do this.** The sealed certificate is self-issued (`CryptoEngine.selfIssuedSenderCertificate`), so **any** authenticated account can mint one naming another account's ACI — it must carry the minter's own key, because libsignal binds the certificate key to the sealer and the receive path then requires that key to be the one the session authenticated. Sealing a `PreKeySignalMessage` under such a certificate therefore succeeds: the address comes from the certificate, `signalDecryptPreKey` saves the minter's identity key against it, and the two keys agree because they are the same key. On the recipient's device the named peer's stored identity is replaced, the verified badge is cleared (2.5 binds verification to the exact key), sending to that peer is blocked until the new key is accepted, and the message is delivered attributed to them with `establishedSession` true. **No code change is available or wanted** — refusing would contradict locked decision §0.2.1, and the identity-change warning *is* the protection — but this row framed the residual as a **relay** relabelling a **first** message, and a circle member (§1.8) forcing an identity-change event against an established, verified contact is a different sentence. **Pinned 2026-08-10 by `SealedSenderTests.testAnImpostorCanSealAFirstMessageUnderAVerifiedPeersName`**, with `testAFirstMessageUnderTheSendersOwnNameLeavesAVerifiedPeerAlone` as its control, so "a stranger's first message" cannot be what moved the peer's identity. Writing it **corrected the mechanism above, which was wrong in a way that mattered**: minting the certificate is not sufficient, because the pinned libsignal takes the sender's address at `signalEncrypt` *and* at `signalDecryptPreKey` and binds both into the message. A container whose certificate names B over a ciphertext encrypted under the minter's own address is refused — the receive path reads the address out of the certificate and hands it to the decrypt, and the MAC then fails — so the impostor must claim the name in **both** places. Neither is checked against anything, so the binding costs consistency and nothing else and the residual stands; but it is a guarantee held by the dependency rather than by this code, and `testTheCertificateNameAndTheCiphertextMustAgreeOnTheSender` pins it so a libsignal release that stopped binding the address surfaces there rather than as a quietly wider residual. The first attempt at this test (PR #80) failed inside its fixture for exactly this reason and was withdrawn rather than debugged.   **Resolved to ACCEPTED 2026-08-12 (P9.S09), with no code change and none wanted.** Every fix that would close it contradicts something load-bearing: refusing an identity change contradicts locked decision §0.2.1, whose whole argument is that refusing receipt drops messages silently and trains users to ignore the warning; and verifying the certificate's *name* would need a server-issued scheme that P7.S01 established cannot be built here (XEd25519 over Curve25519, a Go relay, an iOS-only libsignal pin). **Compensating controls, all tested rather than asserted:** the relay is no longer the attacker — sealing put the label inside the ciphertext and re-wrapping a captured payload is refused (`SealedSenderTests.testAThirdPartyCannotResealAnotherAccountsCiphertext`); the addressed session-establishing frame, the one path where the label was chosen by the deliverer and checked by nothing, is refused outright (`testAnAddressedFirstMessageIsRefused`); an impostor must claim the name in the ciphertext *and* the certificate, which the dependency binds and `testTheCertificateNameAndTheCiphertextMustAgreeOnTheSender` pins so an upstream change surfaces there; and the attempt is **loud** — it replaces the named peer's stored identity, which clears the verified badge, blocks sending until the exact new key is accepted, and shows the identity-change warning, with `testAFirstMessageUnderTheSendersOwnNameLeavesAVerifiedPeerAlone` as the control that an ordinary stranger does not move a verified peer. **What is accepted:** a member of a five-person invite-only circle (§1.8) can force an identity-change event against a verified contact. They cannot read anything, cannot impersonate silently, and safety numbers are the control the user is directed to — which is what §1.8 already says is in scope for that adversary. | ACCEPTED | §1.1, §1.8 | resolved P9.S09; no fix available that does not contradict §0.2.1 |
| 3.9 | **The relay is still told who is sending, by the bearer token on `POST /v1/messages`.** Split out of 3.4 when that closed (P7.S01) rather than closed with it: 3.4 is about what a **seized** database can prove, and the envelope no longer proves anything, but a **live** relay authenticates every send and sees the recipient in the same request. It can therefore build the social graph in the moment, and only a policy — not a mechanism — stops it writing that down. The logs it keeps do not: the send handler deliberately logs neither account nor envelope, and `TestNoLogLineIsEmittedPerDeliveredMessage` fails if five sends ever produce five lines, because a line per delivery is a delivery record that outlives the message. So this is an *observation* an operating host can make, not a record it keeps. **Closing it needs unauthenticated delivery**, which Signal gates on an unidentified-access key derived from the recipient's profile key. Cipher has no profile keys and no way to hand a peer an access token before the first message, so an anonymous send endpoint would also need an abuse control that does not exist yet: without one it is an unauthenticated write to a stranger's queue. **That work is not an approved step**, and inventing one here is exactly what the roadmap forbids — it is named as the recommended next unit instead.   **Resolved to ACCEPTED 2026-08-12 (P9.S09), and the ledger was the last document still calling it OPEN.** `THREAT_MODEL.md` already carried it as a residual in two places — §1.1's *Residual / accepted exception* row names it directly (*"a seized-but-running box … can observe who is sending to whom from that moment on even though it holds no such record"*) and §3.2 states that sealing a frame is not the same as hiding the sender from a live relay. So this row and the threat model disagreed about the same fact, which is the drift this ledger exists to prevent. **Compensating controls:** the distinction the threat model draws is observation versus record, and the record side is enforced — the send handler logs neither account nor envelope, and `TestNoLogLineIsEmittedPerDeliveredMessage` fails if five sends produce five lines, because a line per delivery is a delivery record that outlives the message; the access log carries the route *pattern* and never a populated path (5.33); IP addresses are kept 24h and never correlated with accounts (§7); delivered messages are deleted in the acknowledging transaction (§4); and the stored frame names nobody at all (3.4), so a seizure of the disk proves nothing about who sent what. What remains is what an actively hostile *operating* host can watch in the moment. **What is accepted, stated without softening:** a live relay authenticates every send and sees the recipient in the same request, so it can build the social graph as it runs, and only policy — not mechanism — stops it writing that down. Closing it needs unauthenticated delivery, which Signal gates on an access key derived from a profile key; Cipher has neither, and an anonymous send endpoint without one is an unauthenticated write to a stranger's queue. That is a protocol design of its own and is now **owned by P10.S06** rather than left unscheduled. | ACCEPTED | §1.1 | P10.S06 |

## 4. Storage

| # | Item | Status | Adversary | Fix in |
|---|---|---|---|---|
| 4.1 | **Records are AES-GCM sealed under a Keychain key, bound to their slot by the AEAD's authenticated data**, and the container is excluded from backup. Tampering, relocation between slots and a wrong key all surface as `corruptRecord`, never as "not found". Tested. | CLOSED | §1.4 | — |
| 4.2 | **Record keys derive from peer-supplied strings and never become path components.** Current records use keyed blind database tags; legacy filenames are `base64url(SHA-256(kind ‖ 0x00 ‖ key))`. The true key remains inside the sealed value, and the app-facing database API cannot address internal `proto-*` namespaces. Tested with a traversal payload and `testProtocolNamespacesAreReservedFromTheAppSurface`. | CLOSED | §1.4 | — |
| 4.3 | **Message bodies are sealed in a queryable database.** P5.S10 gave them a home because it had to (an envelope decrypts exactly once, so acknowledging what is not durably stored loses it permanently); what it could not give them was a query shape, and this row recorded that the residual was **performance and query shape, not confidentiality**. **Closed 2026-07-30 (P5.S11):** `SealedRecordDatabase` — SQLite from the SDK, so no new dependency (`THREAT_MODEL.md` §4.1) — in the same container, on the same queue, every value AES-GCM sealed with the AAD binding it to `(namespace, group, ordinal)`. Its keys are HKDF subkeys of the **existing** record encryption key, so one Keychain item is still the cryptographic erase for protocol state and message bodies together; a second Keychain item would have made `destroyAllState` erase half of it. Invariant: **no message body, nickname or peer identifier exists in plaintext anywhere in the container.** Peer ids are a keyed blind index (`HMAC-SHA256`), not a column — a plaintext ACI column would have been the social graph, and a *bare* hash would be reversible by anyone enumerating the ACIs the relay routes to. Guards: `testNoPlaintextBodyIsAtRestAnywhereInTheContainer` and `testThePeerIdentifierIsNotAtRest` scan **every file in the container**, `-wal` and `-shm` included, each carrying a positive control because "found nothing" is also what a broken scan reports (**R2**); `testTheStoreIsUnreadableWithoutTheKeychainKey`; `testARowCopiedToAnotherSlotFailsToOpen`. **One defect found by these tests before commit, worth recording because the mechanism is not obvious:** a wrong key derives a different *index* key, so every lookup missed and reads returned `nil` — unreadable state degrading to "not found", which is exactly what 4.9 forbids and which the file store cannot do because its filenames are unkeyed. A device whose Keychain item was replaced would have reported an empty conversation list and started writing beside the unreadable rows. Fixed with a key-check row at a slot whose tag is a fixed constant, verified on open, so the container refuses to open rather than reading as empty. | CLOSED | §1.4 | P5.S11 |
| 4.11 | **An index lets whoever holds the container group rows and count them.** The cost of 4.3's query shape, stated rather than left implied: rows carry a blind group tag, so an attacker learns "three conversations, of 200, 40 and 5 messages" without learning who any of them is. The previous hash-per-file layout did not permit even that, because each filename mixed in the ordinal, so the container revealed only a total. **Why it is accepted:** the alternative is not having an index, which is the residual 4.3 existed to close; message counts are not content and not identities; and reaching the container at all already means code executing on the device after first unlock (2.1), an adversary who by then has better options. It is a real narrowing of metadata resistance, not a non-issue, and it is the number to re-examine if per-conversation padding or a fixed-size row scheme is ever considered. | ACCEPTED | §1.4 | — |
| 4.4 | **A notification-service extension still cannot share this store.** The first blocker is now closed: protocol records and archive rows share SQLite, and one `BEGIN IMMEDIATE` encloses decrypt plus archive, so an interrupted writer rolls back instead of leaving half a ratchet. The database lock is also the right cross-process primitive, but the killed-writer/two-process case has not been exercised and must be proved before an NSE relies on it. The remaining hard blocker is unchanged: the Keychain item has no access group, correctly — adding one widens reach to another process and requires re-arguing 2.1, plus an App Group migration and rollback plan. Do not add an NSE until those boundaries and the cross-process tests land together. **Resolved as ACCEPTED 2026-08-08 (P8.S04) by withdrawing the requirement rather than meeting it: Cipher will not have a notification-service extension.** The premise this row and 2.1 both rested on turned out to be wrong. An NSE is not what needs to decrypt while the device is locked — **wake-only push does, and it runs in the app's own process.** P8.S02's payloads carry no content, so the sequence is: silent push wakes the app, the app fetches, decrypts and posts a *local* notification. That needs the Keychain reachable while locked, which 2.1 already provides, and it needs no second process, no App Group, and no Keychain access group — so none of the three blockers recorded here is on the path to notifications at all. **ACCEPTED and not CLOSED, because nothing was fixed:** the killed-writer/two-process case is still unexercised and the Keychain item still has no access group. Both remain exactly as true as before; they simply stopped being in the way. **The prohibition therefore stands unchanged** — an NSE still may not be added until the cross-process tests and the access-group argument land together, and adding one now means re-opening this row and 2.1 in the same change. **It is not only prose:** an App Group needs an entitlements file, and `verify-app-target-manifest.sh` tracks `.entitlements` — measured, not assumed, by adding one and watching the gate exit 1 naming `+Cipher/Cipher.entitlements`, then 0 once removed. **What this costs, stated rather than buried:** notifications become best-effort. iOS throttles silent pushes and delivers none at all to a force-quit app, so a visible notification is not guaranteed the way an NSE's would be. That is the trade — guaranteed delivery in exchange for a second process holding the identity key — and it is reversible at the price of re-arguing both rows.  **Code comments corrected 2026-08-09 (audit campaign).** P8.S04 corrected this row and `THREAT_MODEL.md` §1.4 and left six comments in the source still arguing from the withdrawn premise — `SecretStorage` (twice), `DeviceIdentity`, `CipherProtocolStore`, `SerialGate` and `SessionCredential`. Two of them sit directly above the accessibility attribute and the load-or-create primitive they justify, which is exactly where the reader this row warns about arrives. Each now names the real requirement — wake-only push in the app's own process — or, where the withdrawn premise was the only reason (`CipherProtocolStore`'s no-caching rule, the two atomicity arguments), a true one: read-through is what makes `withDecryptedMessageTransaction`'s rollback meaningful, and create-or-adopt is the correct shape for an operation the Keychain is the only place to make atomic. No behaviour changed. | ACCEPTED | §1.4, §1.5 | P8.S04 |
| 4.5 | **The app-switcher snapshot was of whatever was on screen** — iOS photographs the window on every switch away, with no user action, and writes it into the app's container. `RootView` overlays an **opaque** redaction whenever `scenePhase != .active`. Unconditional, unlike the opt-in app lock, because the snapshot happens either way. Opaque rather than blurred: a blur of legible text is often readable at card size, and `UIBlurEffect` renders *from* the content being hidden. Residual: does not stop a deliberate user screenshot (6.4). | CLOSED | §1.4 | P3.S04 |
| 4.6 | **Copying put plaintext on the general pasteboard.** `UIPasteboard.general.string` opts into Universal Clipboard, forwarding a decrypted message to every Mac and iPad on the same Apple Account. `SecurePasteboard.copy` sets `.localOnly` and a 90-second `.expirationDate`. Residual, not overstated: any app the user pastes into receives the text — that is what copying is. Expiry bounds the window rather than closing it. | CLOSED | §1.4, §1.5 | P3.S03 |
| 4.7 | **The local user's own profile fields are sealed, not in `UserDefaults`.** Display name, username and "about" used to sit unencrypted in the app container — not secrets, but they identify the device's user, which is what a seized or stolen phone should not volunteer. P5.S10 had already moved the harder half (the names given to *peers*, and the list of who they talk to — the social graph) into sealed conversation records. **Closed 2026-07-30 (P5.S11):** `ProfileArchive` stores all three as one sealed row in the same container, via 4.3's database. One row rather than three, because they are written together by one screen and there is no state in which the name has changed and the username has not. Invariant: **no profile field in `UserDefaults`, including on a device upgrading from a build that put one there.** The migration reads the plist values, seals them, and *then* removes the keys — in that order, so an interruption leaves them readable rather than gone. Guards: `testEditingTheProfileWritesNothingToUserDefaults` and `testValuesLeftByAnOlderBuildAreSealedAndThenRemoved` assert the plist is empty under all three keys by name (named literally rather than read from the type, so a rename cannot make the test check nothing); `testTheProfileIsNotInPlaintextInTheContainer` scans the container with a positive control. Sign-out deletes the row rather than blanking it. | CLOSED | §1.4 | P5.S11 |
| 4.8 | **The provider's daily snapshot retains what the relay deletes, for up to 24 hours, and cannot be turned off.** OVH's whole-disk VPS backup is not optional on this product (confirmed 2026-07-29). It images the whole host, so a message deleted on acknowledgement at 14:00 still exists in the 03:00 snapshot until it rolls. **This is the one place `BACKEND.md` §4's "acknowledged means gone" is true of the database and not of the host.** Scope corrected by CLEANUP.S13: the image can contain message/attachment ciphertext, public identity/prekey material, account and routing metadata, server configuration/secrets, and TLS private keys; it does not contain plaintext message content or private E2E keys. `Scripts/verify-doc-key-boundary.py` self-tests and rejects the retired blanket claims across current first-party documentation. The snapshot does not widen the adversary set (OVH already holds the live disk and process under the same legal reach); it widens the retention window. **Volume encryption does not mitigate it** — a hypervisor snapshot of a running system captures a filesystem whose key is in memory. Real mitigations are a provider whose snapshots can be declined, or not writing the data at all; neither is a P5 change. | ACCEPTED | §1.1 | Re-argue at P9.S01 |
| 4.10 | **Actor isolation does not make a read-modify-write atomic, and the failure mode was a silently lost message.** Appending to a conversation reads the next ordinal from storage, writes the message under it, and writes the counter back — a sequence that spans two `await`s. An `actor` releases itself at every suspension, so two appends interleave there, both read the same ordinal, both write to the same slot, and **one message is gone with no error anywhere**, because each write succeeded. Under the receive path that message has already been acknowledged, so the relay has deleted the only other copy. Fixed by `SerialGate`, a FIFO async mutex the archive holds for the whole sequence; it is a class guarded by a `Mutex` rather than an actor, because sending the critical section *to* an actor is what strict concurrency (correctly) refuses for a closure touching another actor's state. Negative-tested: with the gate bypassed, 25 concurrent appends store **1** message. In-process only — the cross-process lock an NSE needs is 4.4's item 2 and is not this. | CLOSED | §1.4 | P5.S10 |
| 4.12 | **The receive ordering was not atomic: decrypt committed the ratchet before the archive commit.** A database failure after a successful decrypt left the relay envelope unacknowledged but permanently undecryptable; on retry it entered the intentional “permanent decrypt failure” branch and was acknowledged/dropped. The old test asserted only “no ACK on the first failure” and therefore passed against the loss. **Closed 2026-07-31:** all protocol records moved behind `DatabaseRecordStore` on the archive's SQLite connection; `withDecryptedMessageTransaction` encloses libsignal callbacks, payload classification, blocked-peer handling and archive append in one commit. Existing sealed files migrate lazily; authenticated tombstones prevent a deleted prekey/session from being resurrected if legacy cleanup is interrupted. Repository send/register/receive operations are FIFO-serialised across `await`s, and cancellation removes a queued waiter. Corrupt, oversized or newer-version protocol state is classified as unavailable local storage — never as a permanently bad envelope that may be acknowledged — and the app-facing row API refuses the internal `proto-*` namespaces. Guards: `testAReceiveTransactionFailureRollsBackTheRatchetSoRetryStoresAndAcknowledges`, `testConcurrentReceivesAreSerializedBeforeEitherFetches`, the two `DatabaseRecordStoreTests`, the cancellation test, `testCorruptProtocolStateSurfacesAsStorageUnavailable`, and `testProtocolNamespacesAreReservedFromTheAppSurface`. Each was negative-tested against its defect; the test names appeared in output. | CLOSED | §1.2, §1.4 | P5.S10 remediation |
| 4.13 | **Local erasure, replacement and migration are crash-safe. Closed 2026-08-01 (P5.S11 remediation).** The Keychain service is now the **first irreversible operation** in `destroyAllState`, removing identity and record keys in one platform call; only then is the closed crypto root unlinked. An engine which has begun erasing refuses every other operation but may retry physical cleanup. If a crash lands after key deletion, open refuses any non-empty crypto root without minting a replacement key — including artifacts from a newer format — and the persisted `.destroying` account gate finishes deletion without opening it. SQLite uses verified `secure_delete=ON`, checkpoints with `TRUNCATE` after deletes **and replacements**, and performs a one-time, marker-after-success `VACUUM` so residue created by older builds is scrubbed too; a failed post-commit checkpoint is retried before the next operation and on open, without reporting a committed inbound ratchet as rolled back. Profile edits have one latest-snapshot writer, and an account generation prevents a cancelled old writer from touching a later account. Every committed database protocol value/tombstone is also a durable legacy-cleanup plan scanned on open, so a failed post-migration unlink is retried even though the archive marker is already committed. The earlier account-isolation closure remains: `.destroying` hides the account, plaintext view models are dropped before storage work, and another invite is unreachable until erase completes. Guards: `testKeychainFailureCannotDeleteFilesBeforeCryptographicErase`, `testCiphertextBesideADestroyedKeyDoesNotMintAReplacement`, `testUnknownCryptoArtifactWithoutAKeyDoesNotMintAReplacement`, `testInterruptedCryptographicEraseIsFinishedWithoutOpeningCiphertext`, `testAccountCleanupFinishesPersistedEraseWhenCiphertextCannotOpen`, `testLogicalDeletionScrubsTheMainDatabaseAndTruncatesTheWAL`, `testTransactionalDeletionScrubsCommittedResidue`, `testOpeningAPreHygieneDatabaseScrubsHistoricalDeletedCiphertext`, `testCommittedInboundTransactionDefersABusySecureDeletionCheckpoint`, `testRapidProfileEditsCannotLetAnOlderSaveOverwriteTheNewestSnapshot`, and `testCommittedLegacyCleanupIsRetriedOnReopen`. Each new control was removed/reversed independently; its named test failed before restoration. | CLOSED | §1.4 | P5.S09, P5.S11 remediation |
| 4.14 | **Client-controlled local storage is bounded. Closed 2026-08-04 (P5.S11 remediation).** Invariant: no peer can grow this device's store without limit, and no peer can cause the loss of a conversation it is not part of. Three bounds, enforced together because none implies the others — at most 256 conversations, at most 5 000 messages each, and a 192 MiB logical ceiling on the container measured as `(page_count − freelist_count) × page_size` so that freeing pages actually lowers it. Retention raises `firstOrdinal` and never rewinds the counter. Eviction always takes from the **largest** conversation, so a flooding peer trims its own history first, and never below a floor of 8, so it cannot delete the message whose own append triggered it. All of it runs **inside the append's transaction**, so acknowledgement still means durable. A first message from a new peer at the conversation cap is `IncomingDisposition.quotaExceeded`: dropped and acknowledged with the ratchet committed, in the family of `blocked`, never a thrown storage failure — refusing the ack would leave the relay redelivering an envelope that can no longer decrypt and would stop every message queued behind it. Guards: `ConversationQuotaTests` (6), `SealedRowStoreTests` quota trio, and two receive-path tests driving real envelopes; all eleven negative-tested by name. | CLOSED | §1.2, §1.4 | P5.S11 remediation |
| 4.15 | **The storage bound costs two residuals, both bounded and neither silent.** (1) An attacker holding many identities can occupy conversation slots, after which a first message from a genuine new contact is dropped — logged, but with no UI, so the user is not told. (2) Eviction trims the largest conversation, so an attacker who spreads across many slots rather than flooding one can make a legitimate long conversation the largest and cost it its oldest messages. Both are strictly preferable to the alternatives: evicting whole conversations would be a remote delete primitive, and refusing the acknowledgement would wedge all delivery (4.14). Each attacker identity costs a prekey from the published bundle, so this is not free. The user-facing half lands with the P5.S13 UI pass. | ACCEPTED | §1.2, §1.4 | P5.S13 |
| 4.16 | **A restore resurrects session tokens revoked since the backup was taken.** Opened and accepted 2026-08-11 (P9.S05), by the step that created it rather than found it. `session_tokens` is in the backup set because it has to be: `POST /v1/auth/rotate` needs the old token and redemption mints a *new* account, so an account whose token hash is gone cannot authenticate again by any route — its owner would have to redeem a fresh invite, receive a different `aci`, and re-verify safety numbers with every peer. Excluding the table would make a restore worse than no restore. The cost is that a sign-out or `DELETE /v1/auth/all` performed *after* the backup is undone by it: the hash returns and the token the client still holds authenticates again. **Bounded three ways, none of them a promise.** Expiry is inside the restored row, so anything past `expires_at` is refused on read and swept on the next start (`internal/sweep` runs once immediately, not after the first interval). The window is the backup interval, so RPO bounds it — 24 hours at the documented cadence. And the operator can close it deliberately after any restore by revoking all sessions, which the runbook's restore procedure names as a step rather than leaving to judgement. **Not mitigated by encrypting the backup**, which is why it is a row and not a footnote: the resurrection happens after legitimate decryption, so every control on the archive's confidentiality is orthogonal to it. Re-argue at P9.S01 alongside 4.8. | ACCEPTED | §1.1, §1.8 | P9.S05 |
| 4.9 | **A corrupt record is fatal rather than self-healing, by design.** Silently treating a damaged session as "no session" would be a free session-reset primitive for anyone with write access to the container. Recovery is a deliberate, user-initiated reset. *(Was a second 4.4 until 2026-07-29.)* | ACCEPTED | §1.2, §1.4 | — |

## 5. Transport, server, and UI honesty

| # | Item | Status | Adversary | Fix in |
|---|---|---|---|---|
| 5.1 | **The production messaging path works between two installations through the deployed relay. Closed 2026-08-07 (P5.S14).** Invariant: authenticate, publish prekeys, establish a session from a fetched bundle, encrypt before send, decrypt after fetch, store sealed, and acknowledge only what is already durable — end to end, against the real relay rather than a stub, with no plaintext on the wire or at rest on the relay. **Field evidence (P5.S13, 2026-08-07):** a real message crossed **both ways** between a physical iPhone and the simulator through the deployed relay, each side initiating one session so both halves of PQXDH establishment were exercised rather than one direction replayed; a prekey message each way, a curve and a Kyber prekey consumed from the recipient's pool, `POST 202` then fetch then `ack 200`, the queue draining to zero each time, and the stored envelope containing none of the plaintext. Because acknowledgement follows durable storage inside the same transaction, a drained queue is evidence of *decryption*, not merely of receipt — see the P5.S13 roadmap row for the measurements. That run is evidence, not a guard; the guards are automated and named. **Guards:** `MessagingTests.testRoundTripThroughTheFacade` and `testSessionSurvivesAnEngineRestart` (two real engines, not a stub); `MessageRepositoryTests.testASentMessageIsCiphertextTheRelayCannotRead`, `testTheMessageIsDurableBeforeItIsTransmitted`, `testAFetchedMessageIsStoredAndThenAcknowledged` and `testAReceiveTransactionFailureRollsBackTheRatchetSoRetryStoresAndAcknowledges` (the acknowledge-what-is-durable ordering this row's evidence depends on); `CertificatePinnerTests.shippedPinsCoverTheLiveLeaf` for the pinned transport; `SessionCredentialTests.testWritingUserDefaultsCannotAuthenticate` for the auth boundary; and `Scripts/verify-relay-integration.sh` for the relay half. **Residual (a), operator-accepted 2026-08-07 and unchanged by this closure:** the pairing was one real device plus one simulator. That satisfies the step's "do not test only in the simulator" and proves the *protocol* path device-to-relay-to-simulator. It proves **none** of three things, and none may be claimed on it — **device-Keychain data-protection classes** (a simulator Keychain is a file with no Secure Enclave behind it, 2.1), **biometric app lock** (a real `.deviceOwnerAuthentication` against enrolled Face ID, 5.8), and **a Release-signed install** (entitlements are silently ignored on simulator SDKs, which is why 6.6 exists). A second real device is still the only thing that would. **The push clause this row used to carry is not closed here and did not vanish with it:** it is now 5.35, stated in its own terms rather than as a footnote on a finding about something else. It was also mis-cited — this row said "there is no push (P7.S03)", but P7.S03 only *hardens* a push token; delivery is built in P8.S01 and P8.S02. | CLOSED | §1.2, §1.3 | P5.S14 |
| 5.2 | **Signed-in means an active, unexpired, server-issued credential is in the Keychain**, not a `UserDefaults` bool. `SessionCredential` is `WhenUnlockedThisDeviceOnly` and non-synchronizable — strictly tighter than the identity key, because nothing needs it while locked. Its persisted phase distinguishes registration, profile setup, active use and destruction; only `.active` authenticates the UI. There is no production local mint: a real credential comes from redeeming an invite against the relay (P5.S09), and the DEBUG-only development credential is marked in its stored bytes and refused on read by a Release build. What remains in `UserDefaults` is preference state only and none of it is an authentication boundary. `SessionCredentialTests` plants the old key and plausible variants and asserts the app stays signed out or enters mandatory cleanup. | CLOSED | §1.4 | P3.S01, P3.S02, P3.S06, P5.S09 |
| 5.3 | **The app ran entirely on `MockStore`** — an in-memory array of plaintext fixtures with invented contacts, hardcoded `isVerified` flags, and a `sendText` that appended a struct. No screen touched `CryptoEngine`, so "the UI is not wired to the crypto" was the architecture rather than a gap in it. **Closed 2026-07-30 (P5.S10):** `ConversationStore` derives every `Chat`, `Message` and `Contact` from the sealed archive and the relay, and `MockStore` is gone. Its fixtures survive as `ConversationStore.preview()` behind `#if DEBUG`, populating a store whose `isPreviewOnly` flag makes every mutating method a no-op — so a preview cannot write to the container even by accident. Guarded three ways: the app-target manifest (6.5) shows the file set in review, the Release-bundle audit fails on the preview symbols, and `MessageRepositoryTests` asserts the bytes on the wire are ciphertext the peer can actually decrypt. | CLOSED | §1.2 | P5.S10 |
| 5.4 | **Security UI claimed controls that did not exist** — a hardcoded twelve-block "safety number" under the text *"If these numbers match… your connection is secure"*, a "Mark as Verified" button that only dismissed, Face ID copy with no `LAContext`, and write-only toggles. Two users performing the comparison ritual would have seen the same constants and been told they were secure. Now: the verified badge is gated in one place (`VerificationDisplay`), unenforced toggles carry an `UnimplementedNotice`, and the UI-honesty lint fails on a denylist of retired claims across sources *and* the string catalog. **Residual: the denylist stops regressions, not new inventions.** | CLOSED | §1.4, §1.8 | P1.S05 |
| 5.5 | **Group chat UI shipped while groups are cryptographically unreachable** (3.7). Group creation is `#if DEBUG`, and the Release-bundle audit asserts `NewGroupView` and `createGroup` are absent. | CLOSED | §1.2 | P1.S06 |
| 5.6 | **A debug authentication bypass shipped in Release.** Setters of `debugSkipToMain` were fenced but the property and **both consumers** were not, so the binary contained one boolean bypassing onboarding, authentication and the lock together. Property and consumers are now fenced, and `verify-all.sh` greps the built Release binary for all five symbols. | CLOSED | §1.4 | P1.S07 |
| 5.7 | **The authentication gate was expressed twice and could diverge.** `AppSession.Destination` is now the sole definition and `RootView` switches on it. **Enforcement is structural, not a gate** — re-duplicating it would take a deliberate rewrite, but no check would catch it. | CLOSED | §1.4 | P1.S07 |
| 5.8 | **The app lock never re-engaged.** `RootView` now calls `lockIfNeeded()` on every `scenePhase` away from `.active` — on `.inactive` rather than `.background`, because `.inactive` is when the switcher snapshot is taken, so the snapshot is of the lock screen. `unlock(reason:)` clears the lock only after a successful `.deviceOwnerAuthentication`, and `isAppLocked` is `private(set)`: it had been a plain `var` four call sites set directly, so the lock's only authority was that nobody happened to write that line. `AppLockTests` pins cancel/failure/unavailable each leaving the app locked, and that a retry asks again rather than reusing the first result. | CLOSED | §1.4 | P3.S02 |
| 5.9 | **Onboarding asserted three security guarantees in the present tense on the first screen** — including that keys live in the **Secure Enclave**, which they never do: a libsignal identity key is an exportable software key in the data-protection Keychain, and the Enclave implies hardware non-extractability it does not have. Pillars are now stated as design; "Secure Enclave" is on the lint denylist. | CLOSED | §1.4, §1.8 | P1.S05 |
| 5.10 | **Retired UI claims survived in the localization catalog** after the English originals were deleted. See **R4**. | CLOSED | §1.4, §1.8 | P1.S05 |
| 5.11 | **The Czech bundle shipped the debug affordances `#if DEBUG` had removed from Release** — `cs.lproj` named "Skip to App", "Unlock & Show Main", "Demo Controls" and more, verified against the built artifact, while the audit meant to settle this searched only the Mach-O. No bypass was reachable (5.6 fenced the mechanism), but an attacker reading the IPA was handed the names and the existence of an auth-skip path. The Release audit now searches the **whole bundle**. See **R3**. | CLOSED | §1.4, §1.8 | P1.S12 |
| 5.12 | **A LibSignalClient type was exposed in `CipherCrypto`'s public API** (`Envelope.payloadType(for:)`). Two costs: most libsignal types own a Rust pointer freed from `deinit`, so a handle escaping the module can be released off the crypto queue — turning the module's central concurrency argument into a convention — and `0.x` types in our signatures make every bump an app-wide refactor. `Scripts/verify-api-boundary.sh` dumps the built module with `swift-api-digester` and fails on any `LibSignalClient.` qualified name in the public surface. **It reads the compiler's own export list rather than grepping `public func`, which is why it found this at all.** | CLOSED | §1.4 | P2.S01 |
| 5.13 | **Every "this does not work yet" warning shipped in English while the interface shipped in Czech** — the inverse of 5.4 and harder to notice, because an absence in a catalog looks like every other untranslated key. A reader gets the whole app in their language and, in a foreign one, the only text whose purpose is to stop them trusting the build. Check E in `verify-localization.py` requires every warning-marked string to be translated into every language the catalog otherwise supports, **with the language set read from the catalog**, so adding a language extends the requirement instead of silently exempting it. | CLOSED | §1.4 | P4.S05 |
| 5.14 | **The GUARD list was English-only**, so a Czech string could promise a passcode check with nothing behind it. See **R4**. | CLOSED | §1.4 | P4.S05 |
| 5.15 | **A reverse proxy would have collapsed the only per-IP rate limit into a single global bucket.** `clientAddr` reads `RemoteAddr`, which behind Nginx is the proxy for every request; `POST /v1/invite/redeem` is 5/hour/IP and is the relay's only per-IP control, so the first caller to spend the budget would have denied invite redemption to **everyone** — a global onboarding outage triggerable by any single address, scanner included. Found before Nginx was installed, so it was never live. **The naive fix is worse than the bug:** trusting a client-settable header lets one caller mint a fresh bucket per request, removing the limit rather than mis-scoping it. `httpx.RealIP` therefore trusts an **address, never a header** — `RELAY_TRUSTED_PROXY` defaults to empty and empty means trust nobody. It reads `X-Real-IP` (which Nginx overwrites) rather than `X-Forwarded-For` (an append-only list where "take the last element" assumes a proxy depth nothing verifies), rejects a comma-bearing value, and normalises `::ffff:` forms and zone suffixes because the bucket key *is* that string. Every rejection falls back to the proxy address, which over-throttles; no input yields no limit. Negative-tested six ways. Trust boundary and the deployment-specific value: `BACKEND.md` §9.2. *(Recorded as 5.12 in commit `7f20c16`; renumbered the same day.)* | CLOSED | §1.2, §1.3 | P5.S05 |
| 5.16 | **Nginx accepted TLS 1.2 while its configuration read as TLS 1.3 only.** nginx negotiates the protocol version from the **default server's** SSL context for the listening socket, before SNI selects a virtual server, so a per-server `ssl_protocols` is applied too late to refuse anything — Ubuntu's stock `nginx.conf` governed it instead. The config was self-consistent and `nginx -t` was happy. `ssl_protocols TLSv1.3;` now lives in the `default_server` block with the reason written at the directive so it is not "tidied" back. See **R2** for why the first test reported a pass. | CLOSED | §1.2, §1.3 | P5.S05 |
| 5.17 | **No CAA record, because name.com cannot create one.** `P5.S04` requires a CAA record restricting issuance to Let's Encrypt. The registrar's DNS editor offers A, MX, CNAME, TXT, SRV, AAAA, NS and ANAME, and no CAA (confirmed by the operator 2026-07-30), so the record cannot be created without either a registrar API token — which `INFRASTRUCTURE.md` deliberately avoids creating, on the grounds that a token that does not exist cannot leak — or moving DNS to a third party that would then see every query for the domain. **What the gap actually is:** without CAA, any public CA that can be induced to issue for `relay.mgchatman.app` produces a certificate chaining to a trusted root. **Why it is tolerable here and would not be everywhere:** the only client is the Cipher app, which pins the SPKI and fails closed (P5.S08), so a mis-issued certificate carrying a different key is refused regardless of which CA signed it and regardless of CAA. The host serves no HTML and returns 444 to any unmatched `Host`, so there is no browser population for whom CAA would be the operative control. CAA would still be worth having — it is defence in depth for the rotation window, and the fallback if pinning ever has to be relaxed — so this is a real residual and not a non-issue. **Registrar CAA support is now a selection criterion** for the P9.S01 production domain, alongside jurisdiction and snapshot policy (4.8). | ACCEPTED | §1.3 | Re-argue at P9.S01 |
| 5.18 | **Authentication was a client-side string-length check.** `AuthFlowView` enabled Continue on `code.count >= 4` and then set a boolean; nothing verified the code, so "authenticated" meant "typed four characters" (C-01). **Fixed 2026-07-30 (P5.S09):** the button only submits, and `InviteRedemption` exchanges the code for a session at `POST /v1/invite/redeem`. A credential exists only if the relay issued it, marked `.serverIssued` — the sole origin a Release build reads back. **Redemption is marked non-idempotent** so `RelayClient` never retries it: an invite is single-use, and a retry after a lost response either spends a second code or creates a second account and discards the token the user needed. Failure copy is deliberately uniform across unknown / spent / expired, mirroring the relay's identical 401, because a more specific message confirms to a guessing loop that a code had once been real. Negative-tested five ways: idempotent-marked redemption is retried, any-status-is-success authenticates a 401, an empty token or aci authenticates, `.development` passes for a real session, and camelCase wire fields silently register id 0. **One of those tests was initially vacuous** — its junk body omitted `token_expires_at`, so decoding failed before the empty-token guard ran and it stayed green against a build that accepted an empty credential; it now varies only the field under test. See **R2**. | CLOSED | §1.4 | P5.S09 |
| 5.19 | **A LibSignalClient error type escaped the module through `throws`, where the API-boundary gate cannot see it.** `CryptoEngine.encrypt` rethrew `SignalError.untrustedIdentity` verbatim — deliberately, so a caller could not mistake an identity refusal for a transport failure. But a thrown type appears in no signature, so `swift-api-digester` finds nothing and `Scripts/verify-api-boundary.sh` passes: the only way for the app to *name* that case was `import LibSignalClient`, which is exactly what 5.12 closed. It is now mapped to `MessagingError.identityNotAccepted`; every local record-store failure maps to `.storeUnavailable`, because corrupt/oversized/newer state is not evidence that a receive envelope is permanently bad. **Residual, stated rather than hidden:** other libsignal errors still escape as opaque `Error` values. That is bounded by behaviour — no caller pattern-matches one, and the receive path treats any unmapped failure as permanent for that envelope — and a new mapped case is only added together with code that depends on it. `PublishedKeysTests` asserts the identity mapping; `testCorruptProtocolStateSurfacesAsStorageUnavailable` guards the storage class. | CLOSED | §1.2, §1.4 | P5.S10 |
| 5.20 | **Redemption returned the account's address and threw it away.** `InviteRedemption` decoded `aci` from the relay's response and no caller recorded it, so a successful redemption produced an installation with a valid session token and no address. Closed in P5.S10 and strengthened by P5.S09 remediation: the ACI is now persisted inside the pending credential before registration, `MessageRepository.register()` adopts exactly that address and publishes prekeys, and every later send/receive refuses unless the credential ACI equals the crypto engine's local address. The address is adopted **before** publication, and the publication flag is set **only after the relay accepts**, so a failure resumes after relaunch. A non-UUID or nil UUID response is refused at the boundary. | CLOSED | §1.2, §1.4 | P5.S10, P5.S09 remediation |
| 5.21 | **Six controls in the shipping UI depicted capabilities that do not exist.** Not copy this time — affordances: a Calls tab with fabricated call history and a "simulate incoming call" button; audio and video call buttons in every conversation; an attachment sheet offering five sources that dismissed; a press-and-hold microphone with an animated "Recording…" indicator and an empty action; delivery and read receipts (`MessageStatus.delivered`/`.read`) that no wire format could ever produce; a Linked Devices screen listing an iPad and a MacBook with "last active" times, above a footer admitting it was UI only, while `Envelope` has no `deviceId` at all; and a Storage screen reporting three constants compiled into the app. All are now removed or `#if DEBUG`, on the precedent of 5.5. `Scripts/verify-all.sh` fails if `CallsListView`, `ActiveCallView`, `IncomingCallView`, `AttachmentSheet`, `GroupInfoView`, `MediaGalleryGrid`, the preview fixtures, or their copy appear in the Release bundle — **and now also fails if the search finds no `ChatsListView`**, because "grep found nothing" is what a broken audit looks like too (R2). Calls have a second guard that is stronger than the fence: their fixtures are DEBUG-only, so un-fencing the screens does not compile in Release. | CLOSED | §1.4, §1.8 | P5.S10 |
| 5.22 | **The blob byte quota was charged and never checked, so "500 MB per day" was a number in a document.** After each upload the handler charged one Redis token per megabyte in a loop and **discarded the result**; nothing ever consulted that bucket. The real ceiling was the separate 100-uploads-per-day limit — ten gigabytes of disk and bandwidth per account per day. Two things were wrong and both are fixed: the quota is now checked *before* the write (one megabyte charged up front, the remainder after, because the size is not known until the upload finishes and `Content-Length` is a claim), and the post-charge **saturates** — a token bucket that refuses without deducting leaves the bucket exactly as full as it was before the overrun, so every subsequent upload would be permitted too. `ratelimit.Charge` is that primitive, deliberately separate from `Allow`, which keeps the non-deducting behaviour that is correct for a request that was refused and therefore not served. A limiter error now removes the bytes and refuses the upload: an unmeasurable quota is the same as no quota. Four integration tests, negative-tested by removing the pre-check and by making `Charge` non-saturating. | CLOSED | §1.1 | Security audit remediation, 2026-07-30 |
| 5.23 | **Two authenticated endpoints had no rate limit at all.** `POST /v1/messages/ack` performs a `DELETE … WHERE aci = $1 AND id = ANY($2)` with up to 200 ids, and acknowledging a message that does not exist succeeds by design — so the cheapest possible request was also unbounded, and an authenticated caller could hold a database connection busy for free. `DELETE /v1/blobs/{id}` removes a row and unlinks a file, and was the only authenticated write on the service with no ceiling. Now 120/minute and 200/hour per account, sized against what the client actually does (a receive cycle drains at most 20 pages, acknowledging once per page) rather than guessed. Both keyed by the **caller**, never by the id: a per-id limit is no limit when the caller supplies the id, and it would mint one Redis key per attachment anyone probed. Two integration tests, each negative-tested by removing the check. | CLOSED | §1.1 | Security audit remediation, 2026-07-30 |
| 5.24 | **Every relay restart reset every rate-limit bucket.** The limiter's subject pepper was generated per process, so anything that could restart the relay — a crash loop, a deploy, an OOM kill — handed every caller a fresh allowance, including the invite-redemption brute-force limit and the prekey-drain limit that AUDIT 3.1 depends on. It is now `RELAY_RATELIMIT_PEPPER`, and the trade-off is stated rather than resolved by fiat: configured means buckets survive restarts but bucket keys are stable for the lifetime of the value, so two Redis dumps can be correlated. Optional, therefore, with a **startup warning when it is unset** — previously the weakness was visible only to someone reading `main.go`. A deployment that is not restarted casually should set it. **Set on the staging box 2026-07-30**, and the attempt is worth recording because it failed first: the box was four relay-affecting commits behind, so the binary had no pepper support and the key was absent from its `.env` — which a deploy does not regenerate, `.env` being gitignored and machine-local. `RUNBOOK-VPS.md` H.0b now appends when the key is missing rather than only filling an empty one, and states the R2 trap in its own verification: `grep -c RATELIMIT_PEPPER` returns 0 on a build too old to emit the warning, which is the same number that means success. Observed 0 → 1 → 0. | ACCEPTED | §1.1 | Security audit remediation, 2026-07-30 |
| 5.25 | **Registration and credential lifecycle are now recoverable and account-bound. Closed 2026-07-31 (P5.S09 remediation).** Relay redemption generates the token first, then invite deletion, account insert and initial token-hash insert commit in one transaction; a forced session constraint failure leaves no account and the same invite remains usable. The Keychain credential is versioned and binds token + ACI + issue/server-expiry + persisted phase. `.registering` is stored before address adoption, resumes after crash, publishes exactly once, then moves through reachable `.profileSetup`; only `.active` reaches main. Expiry is enforced locally, including a deadline task for an app left foregrounded; token/ACI/timestamp shapes are bounded, and rotation starts seven days before expiry, is single-flight and non-retrying because the server consumes the old token. A 401 or unreadable/expired credential enters persisted destructive cleanup; a temporarily unavailable `WhenUnlocked` value is re-read from the visible cleanup screen before anything is erased. Another invite stays unreachable until protocol state, sealed history/profile and in-memory conversations are erased. Every send/receive also checks credential ACI == crypto local ACI. Negative tests failed by name when redemption committed before its session, an account mismatch guard was removed, in-memory history survived cleanup, pending/expired credentials counted as authenticated, foreground expiry did not persist destruction, protected bytes were not re-read, concurrent/non-idempotent rotation was allowed, or hostile token/ACI/expiry shapes were accepted. | CLOSED | §1.2, §1.3, §1.4 | P5.S09 remediation |
| 5.26 | **The pinned client trusted the shape of a hostile answer. Closed 2026-08-05 (P5.S08/P5.S09 remediation).** Pinning decided who may speak; nothing decided what they may say. **Bytes.** `URLSession.data(for:)` buffered an unbounded body, so a chunked reply that never ends — free for the relay to serve — cost this device memory until it was killed, and nothing above the transport could intervene, because by the time a `Data` exists for `JSONDecoder` the bytes are already resident. `BoundedResponseLoader` counts what arrives and cancels the task at the ceiling: 64 KiB by default, and a fetch-specific ceiling **derived** from `api.maxFetchBatch` × base64(`store.MaxEnvelopeBytes`) rather than picked. A declared `Content-Length` over the ceiling is refused before a byte is read — **an early-out, not the control**, since a chunked response declares nothing and the party declaring it is the party sending the body. **Redirects** were followed by default; a 302 to another host would have re-sent the bearer token wherever the relay pointed. Refused, and the 3xx is handed back as an unrecognised status. **Cancellation** was swallowed by `try?` in the backoff and then arrived as `URLError.cancelled`, which `classify` maps to `secureConnectionFailed` because a refused pin arrives the same way — so a user leaving a screen was told the connection had been attacked. The loader now turns task cancellation into `CancellationError` and every domain wrapper rethrows it; the pin conflation is untouched for every other case. **The deadline.** `resourceTimeout` bounded one `URLSessionTask` while a call makes up to three with backoff between, so the constant named a bound the code did not have; it is enforced against `ContinuousClock` — monotonic, so a clock correction cannot extend or collapse a call in flight — and clamps both the per-attempt timeout and every sleep. **Origin.** The base must be a bare `https` origin (no credentials, port, path, query or fragment) and the composed URL must stay on it; a request path must be absolute and free of `..`. Credentials matter because `URLSession` turns them into an `Authorization: Basic` header that displaces the bearer token; a port matters because the pin is keyed by host. **Semantic bounds.** A fetch batch may not exceed the relay's own cap, repeat an id, or carry an envelope outside `store.Min/MaxEnvelopeBytes`; an acknowledgement over `maxAckBatch` never leaves the device and a reported acknowledged count above what was asked is refused; a publication reporting an empty pool after an upload is refused. **Invite codes** are validated against the relay's own format before being spent, because `POST /v1/invite/redeem` is the only per-IP limit there is (5.15) and it is the only flow an unauthenticated install has — four typos would otherwise cost an hour of onboarding; `InviteCode` mirrors `invite.Parse` exactly, Crockford substitutions included, so it can never refuse a code the relay would take. Guards: `RelayTransportTests` (33), each negative-tested by reintroducing its defect and confirming the named test failed. **Two of those negative tests found holes in the tests rather than the code, which is the point of running them** (**R2**): the origin checks were asserted on `requireBareOrigin` directly and stayed green when the *call site* was deleted, so a composition-path test was added; and the challenge-absence assertions were first written with hand-typed selector strings, where the ObjC names are `didReceiveData:`/`didReceiveResponse:` — a `!responds(to:)` against a selector that exists nowhere passes for the wrong reason, so every selector is now derived by the compiler and the session-level one carries a positive control. **Three residuals, stated rather than implied.** (a) The publication check bounds a number **the relay chooses**: counts must be non-negative and not zero after an upload. It catches a broken or misrouted relay, not a lying one, and deliberately does not require “at least as many as I sent” — the relay counts after the publish transaction commits, so a peer dispensing one in between is legitimate and a stricter gate would fail correct publications (**R2**). (b) `HTTPURLResponse` canonicalises header names today, so storing them lowercased is defence against a platform behaviour this code does not control rather than an observed break; the guard is a unit test on `Response.header`, because an end-to-end test would pass against the case-sensitive lookup too and prove nothing. (c) The bundle response's individual key fields are bounded only by the 64 KiB response ceiling rather than per field, and outbound envelope size is still not checked client-side before a send the relay will refuse with a 400. | CLOSED | §1.2, §1.3 | P5.S08/P5.S09 remediation |
| 5.27 | **Several relay transactions and input bounds were weaker than the API contract. Closed 2026-08-06 (P5 backend remediation).** Seven defects, each at the handler/store boundary. **The migration ran outside the transaction that was supposed to hold it.** `0001_init.sql` opened with `BEGIN` and closed with `COMMIT` while `store.Migrate` had already opened one — and PostgreSQL has no nested transactions, so the file's `COMMIT` ended the *runner's*, and the `schema_migrations` INSERT after it ran unprotected. The atomicity the runner documented was a property of the runner and not of what executed: a failure between the two left a fully applied schema with no row recording it, and the next boot reapplied the file against objects that already existed. The transaction control is gone and the runner now refuses a migration containing any, comments stripped first so the paragraph explaining the rule does not trip it (**R3**). **Two JSON values in one body were read as one.** `json.Decoder` is a stream decoder, so `{"code":"A"}{"code":"B"}` decoded as the first and discarded the second in silence — a request meaning different things to different readers, on endpoints whose decisions are single-use. One `httpx.DecodeJSON` now owns both that refusal and `DisallowUnknownFields`, so a route added later cannot forget either. **Key and registration ids were unbounded.** Both decode as `uint32` into `INTEGER` columns: above 2147483647 they reached PostgreSQL and returned an out-of-range error, turning a malformed request into a 500 and a logged database failure. Between the protocol's own ceiling and that limit they stored *successfully* and were unusable — a registration id over `0x3FFF` or a prekey id over `0xFFFFFF` cannot appear in a bundle a peer's libsignal will accept, so the account authenticated and could never establish a session. Both are now bounded to the ranges the client already mints from (`DeviceIdentity.registrationIdRange`, `CipherProtocolStore.maxPreKeyId`), so the bound refuses nothing a real client sends. **The blob byte quota floored.** `size >> 20` charged an upload of 1 MiB + 1 byte as one megabyte, so a client uploading just under 2 MiB at a time spent half its 500 MiB allowance — short by up to a megabyte per upload, always in the caller's favour. It rounds up. **Explicit blob deletion removed the row before the file**, so a failed unlink left ciphertext on a seizable host with nothing that could ever find it again — not the sweep, not a query. It now matches the retention sweep: bytes first, row second, and a failed unlink refuses with the row intact so the sweep retries at the TTL. **And the rename was not durable.** `Put` fsynced the file and not the directory holding its new name, so a crash could leave the bytes with no directory entry while the attachment row already existed — a slot the relay believes it has, can never serve, and never retries. Guards: `TestEmbeddedMigrationsContainNoTransactionControl` and four scanner tests; five `DecodeJSON` tests; `TestPutFailsWhenTheDirectoryCannotBeFlushed` (mode `0o300` lets the rename through and refuses the directory open, so only the flush can fail); and eleven integration tests at the handler boundary, each paired with the positive control that proves the bound does not refuse legitimate traffic — the registration-id and prekey-id ceilings, a whole-megabyte upload, an ordinary acknowledgement, and a successful delete. Every defect was reintroduced and its named test failed. **Two of those runs corrected the work rather than confirming it** (**R2**): the first `DecodeJSON` used `dec.More()`, which is written for iterating array elements and reports false for a trailing `]`, so `{"code":"A"}]` passed the check meant to refuse it; and the first registration-id test looped six cases from one address and hit the 5/hour invite limiter (5.15), so it was measuring the limiter rather than the bound. **Four residuals, stated rather than implied.** (a) The schema's own CHECKs are unchanged — `key_id >= 0`, `registration_id > 0` — so the tight bound lives in the handler. That is sound while the relay is the only writer, and it is the assumption to revisit if that ever stops being true. (b) The directory fsync is proved to be *called*, not to have persisted: durability across a real power loss needs crash injection this suite does not have. (c) Deletion's remaining window is the smaller one by choice — bytes gone and the row delete failing leaves a row pointing at nothing, which 404s on download and is removed at expiry. (d) A single upload can still overshoot the remaining allowance by up to one blob, unchanged and documented in `BACKEND.md` §5: the size is not known until the upload finishes and `Content-Length` is a claim. | CLOSED | §1.1, §1.2 | P5 backend remediation |
| 5.28 | **Documented relay lifecycle and deployment controls were missing or incomplete. Closed 2026-08-06 (P5 backend/infra remediation).** Four defects, none of which any test or read path could have surfaced. **The 180-day abandonment policy was a number in a document.** `accounts.last_seen` was written on every authenticated request and read by no code at all, so `BACKEND.md` §4's "abandonment sweep at 180 days" described a control that did not exist — and unlike every other retention row there is no expiry predicate that could even make an abandoned account *invisible*, so the symptom was nothing at all. `store.DeleteAbandonedAccounts` is now the fourth task in the hourly pass, comparing against PostgreSQL's own `CURRENT_DATE` (a threshold computed in Go is computed against a clock that did not write the row) and bounded to 500 per pass because one account delete cascades across six tables. It runs **last** for that reason: it is the slowest task and must not starve the three cheap ones of the pass they always complete. Attachments deliberately do not cascade — the id is the whole capability and there is no owner column (§2.8) — and need not, since their seven-day TTL is far inside 180. **The refresh that feeds it failed silently.** `LookupSession` swallowed a failed `last_seen` write entirely, which was harmless while nothing read the column and became an active-account-deletion path the moment the sweep existed: one lost day costs nothing, a refresh broken since some deploy nobody noticed is an account in daily use counting down to erasure. It now returns `ErrLastSeenNotRefreshed` **alongside the valid aci** — the request is still served, because failing it turns bookkeeping into an outage — and the auth middleware warns. **The warning is throttled to once a minute, and that is a privacy control**: a failed refresh leaves `last_seen` stale, so every later request retries and fails identically, and an unthrottled line would be a per-request record of who was talking to the relay, written by the code that exists to avoid keeping that (§7). It names no account, because `aci` is not on the redaction denylist — "aci" contains none of the denied substrings — and would have gone straight to disk. **Blob errors carried the capability into the log.** A blob's file name *is* its id, that id is the entire authorisation to read or delete the attachment, and every `os` failure is a `*os.PathError` or `*os.LinkError` naming it — reaching six call sites in `internal/api` and `internal/sweep` as `slog.String("reason", err.Error())`. The fix is at the source rather than in the denylist, deliberately: `reason` carries the useful half of every error the relay logs, and denying it wholesale is how redaction gets switched off entirely (**R2**). `blob.scrub` rebuilds the error with the path replaced by `logging.Placeholder`, keeping the operation and the syscall message; the rebuilt error still unwraps to the same errno, which `Delete` and `Open` both depend on for their `os.ErrNotExist` branches. **And the datastores were unconfined.** `api` dropped `ALL`, refused new privileges and ran a read-only rootfs while Postgres and Redis ran Docker's default capability set with no ceiling on memory, CPU or processes — protecting the container that holds a static binary and client-encrypted blobs, and not the one holding every account row, public identity key and undelivered envelope. Both now drop `ALL` and add back only what their entrypoints use to own a directory and `su-exec` (`CHOWN`, `SETGID`, `SETUID`, plus `DAC_OVERRIDE`/`FOWNER` for Postgres), refuse new privileges, and carry `mem_limit`/`cpus`/`pids_limit`. Redis is additionally read-only with a tmpfs `/data`. **Redis also gained `--maxmemory` with `noeviction`, which is a security setting**: with only a container limit, a full Redis is resolved by the OOM killer, and with persistence off it returns holding no rate-limit buckets — every caller granted a fresh allowance by traffic rather than by a deploy, which is AUDIT 5.24's harm through a different door. A Redis-side ceiling makes it refuse the write instead, which every limiter call site already treats as fail-closed. `allkeys-lru` would discard those same buckets quietly, so the policy is stated rather than defaulted. Guards: three `internal/sweep` unit tests, four `internal/api` tests on the warning, eleven `internal/blob` redaction tests, four integration tests now required by name in `verify-relay-integration.sh`, and a `verify-relay.sh` compose gate that self-tests 10 cases — eight deletions applied to the *real* file, a commented-out `cap_drop`, and the committed file itself as the positive control. Every one was negative-tested: removing the accounts task named all three sweep tests, dropping `scrub` from `Delete` printed the live capability in the failure, an unthrottled warning produced 50 lines for 50 failures, adding an `aci` field failed by name (**and confirmed the denylist does not cover it**), and unconfining Postgres or unbounding Redis named the exact service and control. **The CVE gate is `Scripts/verify-image-vulns.sh`, and it answers a different question from `verify-vulns.sh`.** That gate scans the source under the toolchain `go.mod` declares; this one extracts `/relay` from the built image and scans it, because the Dockerfile's build stage pins a *different* Go — go.mod says 1.25.12, the builder is golang 1.26.5-alpine — so the shipping standard library was the one nothing asked about, and most of what a Go scan finds is stdlib (AUDIT 1.13). It lives in the Docker gate rather than in `verify-all.sh` because it needs the image. **Binary mode was checked, not assumed:** `-ldflags='-s -w'` looks like it should blind the scan, and a blind scan reports exactly the "No vulnerabilities found" this one prints — so a probe built with the same flags under go1.24.0 was scanned, and reported 44 standard-library vulnerabilities naming reachable symbols with exit status 3. **Three residuals, stated rather than implied.** (a) Postgres is not read-only: its socket, lock files and statistics live outside the data volume at paths that move between major versions, so a rootfs lockdown would be a tmpfs list that rots at the next image bump. (b) The image scan covers the relay image only. Postgres and Redis are third-party images, pinned by digest, and scanning them needs a container-image scanner this repository does not depend on; adding one is a supply-chain decision, not a gate change. (c) The image gate's self-test proves the scan cannot pass without an artifact — missing, empty, or not a Go binary — not that it detects a CVE end to end, because the only honest self-test for that downloads an obsolete toolchain on every run and leaves a vulnerable compiler in the module cache. The measurement above is the evidence instead. | CLOSED | §1.1, §1.7 | P5 backend/infra remediation |
| 5.29 | **The staging HTTP redirect and catch-all virtual hosts inherited a detailed access log. Configuration fixed and gated 2026-08-06; the live box remediated and verified the same day, closing it.** The relay's TLS vhost set a deliberately minimal format and a 24-hour rotation. Its two siblings set nothing and inherited `nginx.conf`'s http-level `access_log /var/log/nginx/access.log;` — no format named, so nginx's built-in **combined**: client IP, the full request line, the referrer and the user agent. Stock Ubuntu logrotate then keeps `/var/log/nginx/*.log` for **14 daily generations**. So the two endpoints an unauthenticated scanner reaches first retained for a fortnight exactly what `httpx.pattern()` strips from application logs: `GET /v1/keys/<uuid>` over `http://` is a metadata record naming who was looked up, and redirecting it to HTTPS does not stop it having been written down first. `server_tokens off` was likewise set only in the TLS vhost, so the redirect and the 444 server announced the nginx version. **Measured on the box 2026-08-06, not inferred:** `nginx.conf:40` carries the bare `access_log`, `nginx.conf:21` has `server_tokens` commented out, and seven rotated generations were present with `access.log.1` at 102 KB. **What changed.** The configuration moved out of runbook heredocs into `server/deploy/nginx/`, so it is reviewable in a diff and checkable offline — a configuration that exists only as a shell snippet in a document is one nothing verifies. Every server block now states its own `access_log` (`off` for the redirect and the catch-all, `minimal` for the relay), because `access_log` directives at one level *accumulate* rather than replace, so the only reliable way to stop a server writing the inherited log is to say so in that server. `server_tokens off` moved to http level. Guard: `Scripts/verify-nginx-config.py`, gate 9 of `verify-all.sh`, which brace-matches every `server` block and fails one that sets no `access_log`, any log naming `combined` or writing under `/var/log/nginx/`, a `log_format` carrying `$request`/`$request_uri`/`$http_user_agent`/`$http_referer`, and a missing `server_tokens off`. Comments are stripped first (**R3**): the files explain the rule using the very tokens it forbids. Self-test: 13 cases, each defect reintroduced — including both halves of the original finding — plus a control that a comment naming the forbidden variables does not fire it, and one that the three server blocks are parsed individually rather than as one. **The box is now remediated, and this was measured rather than reported.** Both files are byte-identical to `server/deploy/nginx/`; three `access_log` directives are present (`off` for the redirect and the catch-all, `minimal` for the relay); `/var/log/nginx/` holds two empty files and no rotated generations, against seven with `access.log.1` at 102 KB before. Traffic to the port-80 redirect and to the bare-IP catch-all left the stock `access.log` at **0 bytes** — and that is only evidence because of its positive control (**R2**): the relay's own `/var/log/cipher/cipher-access.log` grew over the same requests, so the silence is the redirect not logging rather than the box not serving. A minimal line is client IP, method, status, bytes, duration; no path, user agent or referrer. The redirect answers `Server: nginx` with no version. `RUNBOOK-VPS.md` §H.6 records the procedure and the state table the observations. **Residual by design:** the gate reads the committed files, never the box. A check that needs SSH cannot run in CI, and a gate that cannot run is one that gets removed (**R2**) — so §H.6 step 1 is a byte-for-byte diff an operator runs, and drift between repository and host is caught there rather than here. **Three of §H.6's own commands were wrong and could not have verified it** — two bare greps counted the comments explaining the directive they searched for (**R3**), and `sudo wc -c < file` performs the redirection as the unprivileged shell, so it failed with "Permission denied" on a correctly configured box every time. Corrected, with a positive control added. | CLOSED | §1.1, §1.3 | P5.S05 remediation; the live box via RUNBOOK-VPS.md §H.6 |
| 5.30 | **Release presented controls and claims with no mechanism behind them. Closed 2026-08-05 (CLEANUP.S09/S11/S12/S14/S15/S16).** The Invite Friends stub and fabricated/empty codes, false no-server warning and no-op leave action are gone; authenticated relay issuance remains real future client work. Key-custody copy distinguishes public identity/PQXDH keys intentionally published to the relay from private halves in the Keychain, and no longer claims the relay sees only ciphertext—it also sees public keys and routing metadata. Profile-photo no-ops and the placeholder support address are gone; initials and the sealed profile editor remain. Notification onboarding/settings, their write-only preview preference, and the obsolete pre-P5.S10 plaintext warning are gone; `testTheRetiredNotificationPreviewPreferenceIsCleared` proves old values are deleted. Inert appearance, keyboard-send, archive/export, default disappearing timer, redundant session-management and storage surfaces are gone; `testTheRetiredDefaultDisappearingPreferenceIsCleared` proves that old default is deleted. S16 removes the local-only reply UI and dead reply field; per-chat disappearing picker, badges, writers and view-model field; empty production Groups filter; and every unavailable safety-number/registration-lock screen. The sealed schema-1 `disappearingSeconds` member remains decoder-only so cleanup does not become an unauthorized storage-schema change. DEBUG-only P10 group creation/info stays fenced, and sender-key rejection is unchanged. With no unfinished controls left, the unused `UnimplementedNotice` component is gone. The localization gate rejects all retired concepts in English and Czech, exact-matches the group labels so legitimate “digit groups” terminology remains possible for P5.S12, and self-tests every rule. Future implementations remain tracked by their actual roadmap/audit items rather than shipping placeholders.  **The per-chat disappearing picker returned in P6.S03, with a mechanism this time.** 5.30 removed it because the preference was write-only — nothing deleted anything, so the control was a claim. It is back because the claim is now true: the timer travels inside the ciphertext as a `MessagePayload` content type, each message stores an absolute expiry, and a sweep removes the row from a container opened with `secure_delete`. The two `verify-localization.py` denylist entries that banned the words ("Disappearing messages", "Mizící zprávy") are retired with their reasoning, as P5.S12 did for "Safety Number" — banning a phrase that has become true is how a denylist starts costing more than it protects. What replaces them is behaviour: `DisappearingMessageTests`. **"Default Timer" stays banned**, because the *global* preference it named still does not exist. | CLOSED | §1.4, §1.8 | P5 UI remediation |
| 5.31 | **The invite-code-only identity gate matches field *names*, and the client half cannot cover the whole app.** `Scripts/verify-identity-fields.py` (locked decision §0.2.7) refuses `phone`/`email`/`sms`/`username`/`msisdn`/`otp`/`verification code` and friends across the entire relay, the wire format, and `Cipher/Networking`. Three residuals, stated rather than implied. **(a) Names, not semantics.** A field called `contactRef` holding a phone number passes. The gate stops the way this property actually drifts — someone adds `email` because every other messenger has it — not a deliberate attempt to hide one; that is what review is for. **(b) The client scope is `Cipher/Networking` only,** and cannot be widened: the *local* profile legitimately holds a `username` (`ProfileArchive`, client-side only, `BACKEND.md` §2.1), so an app-wide ban would forbid the wrong thing and be deleted the first time it was inconvenient (**R2**). Narrowed by construction rather than left open — anything collected elsewhere in the app must still cross `Cipher/Networking` to reach the relay and must still fit the wire types `LockedDecisionsTests.testIdentityCarriesNoHumanIdentifier` pins, so the *transmission* is guarded even where the *field* is not. **(c) `ServiceIdentifier.Kind.pni` stays representable.** A PNI is a UUID in a second namespace and carries no phone number; the case exists so a PNI-addressed envelope is rejected rather than misparsed, and Cipher never issues one. Not a leak, recorded so the next reader does not have to re-derive that it is not. Both halves negative-tested: the script fires on every one of its six surfaces and stays silent on the same words in a comment (**R3**), and the test fails by name on a phone field, a verification code, a widened identifier and a decoder that stops rejecting one. | ACCEPTED | §1.1 | — |
| 5.32 | **The relay refused every prekey publication a real client could send, and no test could have noticed. Closed 2026-08-06 (P5.S13 field remediation).** Found on staging: a physical device redeemed its invite (201, account created) and then failed at `PUT /v1/keys` with six 400s and a 429, leaving the app stuck at *"Finishing secure setup"* — `MessageRepository.register()` correctly refusing to advance past `.registering`. **The bound that refused it was not the one that validates the request.** `config.MaxRequestBytes` defaulted to 128 KiB and was documented as *"the largest legitimate body is an envelope"*, which stopped being true when prekey publication was wired: an ML-KEM-1024 public key serializes to 1569 bytes, so the shipped pool of 100 makes a body of roughly 229 KB, and `MaxPreKeysPerUpload` declares 200 legitimate. `httpx.LimitBody`'s `MaxBytesReader` failed the read, `DecodeJSON` returned an error, and the handler answered a bare 400 — **the relay refusing a body its own validator declares legal**, with no log line, on a route capped at six publications a day. Because `auth.allow` runs before the body is read, every refusal still spent a token, so the seventh attempt became a 429 and the account was locked out for the day. **The invariant, now enforced by construction:** every body `validate()` accepts must be readable. `api.MaxPublishBytes` is *computed* from the same constants `validate()` checks rather than chosen, so widening a bound widens the ceiling with it; `/v1/keys` joins `/v1/blobs` in owning its own limit through `api.BodyLimitExemptPrefixes`, which main and the tests share so an exemption cannot be dropped in production while the tests still assume it (**R2**). An oversize publication now answers **413, not 400** — the two failures are different and the caller can act on only one of them, and collapsing them is what made this cost a session. **Why nothing caught it.** Two gaps, both closed. The integration stack was a bare `ServeMux`, so the middleware that did the rejecting was never in the path — every publish test proved the handler agreed with itself. And the fixtures modelled a four-key pool of 1568-byte placeholders, so the suite proved the relay agreed with the *test's* idea of a client and never with the real one. Guards: `TestTheShippedClientsPublicationIsAccepted` and `TestABodyAtTheValidatorsMaximumIsReadable` run the production chain against the shipped payload shape and against every bound at its maximum at once, both required by name in `verify-relay-integration.sh`; `TestAnOversizePublicationIsRefusedAsTooLarge` holds the other direction, since an exemption is not "no limit"; `testThePublishedBodyStaysInsideEveryBoundTheRelayEnforces` checks the *real* captured `register()` body against the relay's bounds, with `testTheBoundCheckerReportsAPublicationTheRelayWouldRefuse` as its positive control; and `testSerializedKeyAndSignatureSizesAreWhatTheRelayBoundsAssume` measures 33/1569/64 from LibSignalClient itself, so a `0.x` bump (**1.4**) changes a named assertion rather than production. Negative-tested three ways in the real files: dropping `KeysPathPrefix` from the exemption list and shrinking `MaxPublishBytes` to 128 KiB each fail both Go tests by name, and raising `defaultOneTimePreKeyCount` to 201 fails the Swift bound check naming the pool. **Stated residual:** the ceiling is derived from `maxKyberKeyBytes`, itself a deliberately loose bound, so it admits roughly 1.1 MiB against a real publication's 229 KB. That is the safe direction — `validate()` is what refuses a malformed upload, and this exists only so an oversize read cannot pre-empt it — and the route is authenticated and capped at six publications a day. | CLOSED | §1.2, §1.3 | P5.S13 |

| 5.33 | **The access log named no route for any request that arrived through Nginx, and the test that exists to check it could not see that. Closed 2026-08-07.** `docs/BACKEND.md` §7 promises method, **route pattern**, status and duration; the deployed relay wrote `"route":"(unmatched)"` on every line except the health probes, so route visibility — the half of the line an operator acts on — was absent for the whole of P5. **The cause is an interaction, not a typo.** `ServeMux` records the matched pattern on the request *it* routes; `RealIP` deliberately hands a shallow **copy** downstream so the caller's `RemoteAddr` is never rewritten (`TestRealIPDoesNotMutateTheCallersRequest` pins that), so the mux stamps the copy while `Log` and `Recover`, which sit outside it, still hold the original and read an empty `Pattern`. `RealIP` only copies when a *trusted proxy* supplied the header — which is why `/health/ready` was the one route that looked right: Docker's `HEALTHCHECK` reaches the API directly, sends no header, and never takes the copying path. The one caller that could not exhibit the defect was the only evidence anyone saw. **The fix asks the router instead of reading the field.** `httpx.RouteFor` and `MuxRoute` resolve the pattern through `mux.Handler`, which is a side-effect-free lookup, so the line stays correct however many middlewares copy the request on the way in — a property reading `r.Pattern` cannot have. `Recover` takes the same resolver, because "where it happened" is the entire value of a panic line and it was reading `(unmatched)` too. The placeholder is unchanged for genuine 404s, so an attacker still cannot get a chosen path echoed into the log. **Why the suite was green.** `TestLogRecordsThePatternNotThePopulatedPath` chains `Log` straight onto the mux, with no `RealIP` — the stack under test was not the stack that runs, which is **5.32's failure mode exactly, in a different file.** Guards: `TestLogRecordsThePatternThroughTheProductionChain` and `TestLogHidesAnUnmatchedPathThroughTheProductionChain` build the chain in `main`'s order and drive it through a request shaped the way Nginx delivers one. Negative-tested in the real file by making `pattern` ignore the resolver: the new test fails reporting the literal production symptom, `"route":"(unmatched)"`, **and the pre-existing test still passes** — the blindness demonstrated rather than argued. **Confirmed on the live box 2026-08-07:** a matched route driven through Nginx logs `"route":"GET /v1/keys/{aci}"` where the same shape logged `(unmatched)` an hour earlier, an unregistered path still logs the placeholder, and the probe's UUID occurs 0 times in the log (`RUNBOOK-VPS.md`, 5.33 deploy). | CLOSED | §1.1 | Relay logging remediation |

| 5.34 | **Orphaned local state is named, logged, and recoverable with consent. Closed 2026-08-07.** Invariant: sealed records surviving with no Keychain key behind them are a *distinct, public* refusal — `CryptoEngineError.orphanedLocalState`, translated at `CryptoEngine.init` from the internal `RecordStoreError.missingEncryptionKey` — and the one open failure a user is offered a way out of. The refusal itself is unchanged and must stay: **no replacement key is ever minted beside orphaned ciphertext** (plan §0.2), which is asserted on the failure path itself rather than inferred. Three parts. **Named:** `CipherLog.store` records the engine's refusal and `AppLog.store` records onboarding's, so the device log no longer stops at `Initializing libsignal`; the generic `catch` now logs the error's *type name* — a compile-time identifier, never `error` or `localizedDescription`, which could carry an address or plaintext (§4.6) — and `.public` is required there, because the default privacy for an interpolated string is redaction and would reproduce the empty log. **Distinct:** its own message, separate from the eight words that also cover a malformed relay response. **Recoverable:** `ConversationStore.discardOrphanedLocalState` reaches `CryptoEngine.destroyPersistedState`, behind a confirmation that states the loss. **The authorisation is the guard that matters.** That primitive removes every Keychain secret and unlinks the container without opening anything; against a live account it is silent, total and irreversible. `destroyAccountState` may call it because a persisted `.destroying` gate has already authorised it — onboarding has no such gate, so the authorisation here is a `localStateIsOrphaned` flag latched **only from an observed refusal**, never from a probe, and spent on success. Guards: `CryptoEngineTests.testOrphanedLocalStateIsAPublicErrorDistinctFromDestruction` (pins it apart from `.destroyed`, which is recoverable by reopening and must not offer an erase), the amended `testInterruptedCryptographicEraseIsFinishedWithoutOpeningCiphertext`, `MessageRepositoryTests.testOnboardingSurfacesOrphanedLocalStateAndCanDiscardIt` end to end through a real engine, and `testDiscardingLocalStateIsRefusedUnlessOpeningReportedItOrphaned`. Negative-tested twice in the real files: deleting the translation fails all three engine-facing tests with the app seeing `nil` where it needs the case — the dead end reproduced — and deleting the `localStateIsOrphaned` guard fails the other two reporting "an unauthorised caller reached the irreversible erase" and "the erase ran again against a live account". **Residual, deliberate and narrow:** only the onboarding entry point offers the remedy. If the condition somehow arose after registration, `ConversationStore.record` still maps it to a generic failure — a screen with a live conversation list is the wrong place to offer an irreversible erase, and the field condition arises at launch. | CLOSED | §1.2 | Onboarding-recovery step |
| 5.35 | **Delivery is foreground-only. A message waits on the relay until the recipient opens the app, and nothing tells them it is waiting.** Split out of 5.1 when that row closed (P5.S14) rather than closed with it: 5.1 asserts that the messaging path *works*, which is now evidenced, and foregrounding changes *when* a message is collected, not *whether* the path delivers it. Every delivery in the P5.S13 field run demonstrated this — each one needed the receiving app brought forward by hand. **Why ACCEPTED rather than OPEN:** it weakens no confidentiality, integrity or authentication property, so it has no adversary, and this ledger's own rule is that a finding with no adversary is a bug rather than a security finding. It is carried here anyway because it is a real limitation a user experiences and because burying it inside a closed row is how a known gap stops being known. **The half that *would* have been a security problem is already closed:** the app makes no claim to the contrary. The premature notification promises were retired in the 5.30 cleanup and are held retired by `Scripts/verify-localization.py`, whose DENY list carries "So you know when a friend messages you", "Show Preview" and their Czech renderings, each with a self-test case that fails if the claim returns. Delivery and read receipts do not exist on the wire either, and `ConversationStore` deliberately does not invent them. So the app is honest about this; it is simply not yet convenient. **Fix in P8.S02** — the server APNs provider with content-free, wake-only payloads — which P8.S01 (the APNs key, a human step) gates. **P8.S04 bounded what that fix can deliver:** with no NSE, a notification is posted by the app after a silent push wakes it, and iOS throttles silent pushes and delivers none at all to a force-quit app. So this row **narrows** rather than closes — delivery stops requiring the user to open the app, without becoming guaranteed. P7.S03 hardens the token that work creates and does not itself deliver anything. | ACCEPTED | — | P8.S02 |
| 5.36 | **The launch-time retry of a failed prekey publication was dead code, and could not have worked if it had been called. Found and closed 2026-08-07 (P6.S01).** `MessageRepository.resumeRegistration` documented itself as running "on every launch rather than only after redemption, because a publication that failed the first time leaves an account no peer can start a session with". Nothing outside the tests called it. Worse, it asked for a `.registering` credential, which an account holds only during onboarding — so a caller wired to it would have received `notAuthenticated` on every launch and given up. Both halves were needed to make the retry real, and the second is why this is a finding rather than a missing line: a reviewer checking that the retry existed would have found the method, read its comment, and stopped. The symptom was invisible on the affected device by construction — an account whose keys never reached the relay looks entirely normal to its owner, and the failure surfaces as *other people's* session setup returning 404. Invariant: `maintainKeys` is called from `ConversationStore.start`, which runs when the app reaches the main UI and on every return to the foreground, and it accepts any credential that is not `.destroying`. Guard: `testAnActiveAccountStillRetriesAPublicationThatNeverSucceeded`, which drives a publication the relay refuses, completes onboarding, and requires the retry to publish. Negative-tested by restoring the `.registering`-only credential, which fails that test by name and no other. | CLOSED | §1.2 | P6.S01 |
| 5.37 | **The identity-change banner told the user that safety-number comparison did not exist. Found and closed 2026-08-09 (audit campaign).** The inverse of every other row in this section, and the reason it survived: the honesty gates hunt for a claim of a control that is missing, and this was a *denial* of one that is present. `MessagingFailureBanner` rendered "safety-number comparison is not implemented yet — do not treat this conversation as secure" for `identityNotAccepted`, which is the exact failure a key substitution produces (§1.2, §1.8) — while `SafetyNumberView` had been rendering real digits since P5.S12 closed **2.5**, one tap away in the Info screen the banner never named. So the user was warned at the right moment and then told the remedy did not exist. It shipped in **both languages** (**R4**), and neither existing check could see it: the localization denylist had no entry, and check E's `WARNING_MARKERS` matched it as a *correctly translated* safety warning — coverage was right, the sentence was wrong. Two stale premises in comments came with it, `MessagingFailureBanner` and `RelayKeyDirectory`, both still written as though P5.S12 were pending. **Invariant.** No shipped string, in any language, may deny a control this build has. **Guard:** two `verify-localization.py` DENY entries, English and Czech, each with its own self-test case. Negative-tested against the real files: restoring the English literal in `MessagingFailureBanner.swift` failed the gate with "claims 'safety-number comparison is not implemented'", and restoring only the Czech `stringUnit` failed it with the Czech entry — the half an English-only fix would have left behind. **Residual:** a denylist stops a retired claim coming back and cannot stop a new one, which is 5.4's recorded residual and unchanged here. | CLOSED | §1.2, §1.8 | audit campaign 2026-08-09 |
| 5.38 | **The two nginx directives that closed 5.16 and hold up 5.15 were enforced by a comment. Found and closed 2026-08-09 (audit campaign).** `verify-nginx-config.py` reads exactly the files these live in and asserted neither. **(a) `ssl_protocols TLSv1.3` in the `default_server` block** is what closed **5.16**: nginx takes the TLS version from the default server's context for the listening socket, before SNI picks a virtual server, so a floor set only in the relay block is applied too late to refuse anything and the distribution default governs the socket. Deleting the line leaves a file that reads as 1.3-only, a server that accepts 1.2, and an `nginx -t` that is happy. **(b) `proxy_set_header X-Real-IP $remote_addr`** is the deployment half of **5.15**: `httpx.RealIP` trusts that header only from `RELAY_TRUSTED_PROXY`, which is sound only while the proxy *sets* it from the connection. Point it at a `$http_` variable and the value becomes client-controlled, at which point one caller mints a fresh rate-limit bucket per request — the naive fix 5.15 names as worse than the bug. **Invariant.** Every `ssl_protocols` in `cipher.conf` is exactly `TLSv1.3`, the default server carries one, and every block that proxies sets X-Real-IP from `$remote_addr` and nothing else. **Guard:** three checks in `verify-nginx-config.py`, with five new self-test cases (13 -> 18). Negative-tested against the real `cipher.conf`: removing the default server's floor failed with "the default server does not set 'ssl_protocols TLSv1.3'", and setting X-Real-IP from `$http_x_forwarded_for` failed with "a client-supplied value would become the rate-limit subject"; both exit 1, both restored. **Residual unchanged:** this reads the committed files, never the live box, which is 5.29's own stated limit — the comparison against the host stays an operator step in `RUNBOOK-VPS.md`.  **Narrowed 2026-08-09 after review:** the default-server rule matched any block whose `listen` carried `default_server`, so splitting `listen 80 default_server` and `listen 443 ssl default_server` into two servers — an ordinary refactor — would have demanded a TLS floor inside a plain-HTTP block. It now matches only a listener that is both `ssl` and `default_server`, with a self-test case for the split. | CLOSED | §1.2, §1.3 | audit campaign 2026-08-09 |
| 5.39 | **A recipient's pending queue had no depth or byte ceiling — the one authenticated growth path on the relay with no quota. Closed 2026-08-10.** `EnqueueMessage` checked the envelope length and that the recipient exists; the `messages` table carried no quota column or constraint, and `CountPendingMessages` was test-and-operational support that no handler called. The only bound was the send rate, 60/min/account, against `MaxEnvelopeBytes` 65567 and a 30-day `MessageTTL`: ≈3.75 MiB/min, ≈5.3 GiB/day per sending account, retained 30 days, self-addressed sends included. The recipient could not shed it faster than they fetch and cannot refuse a sender at the relay, since blocking is client-side. Every other authenticated growth path was quota'd — blobs by count and bytes, publication by rate, ack and delete by rate — and 5.22's own words applied here with no quota at all: "an unmeasurable quota is the same as no quota". Postgres filling is also what stops the retention sweep, the highest-value control in the design. **Invariant.** A recipient's pending `octet_length(envelope)` never exceeds the configured ceiling, and a message refused by it is indistinguishable, in status and body, from one that was stored. The check rides inside `EnqueueMessage`'s existing statement — no separate read to widen the window, no new index (`messages_recipient_idx` already serves the lookup, and a covering index would put envelope bytes in a second structure inside the table a seizure is after), and no quota column (a stored total is a second copy of what the rows already say, and it drifts). **The ceiling is `RELAY_MAX_PENDING_BYTES`, defaulting to 32 MiB**, argued at `api.DefaultMaxPendingBytes` and in `BACKEND.md` §5 rather than picked round: ≈48,000 typical ~700-byte sealed envelopes or ≈510 at the maximum size, which an honest recipient reaches only by staying offline while ~1,600 messages a day are addressed to them for the whole TTL, while a hostile account fills one queue in under nine minutes and is then refused. It is configurable because a ceiling nobody can measure is 5.22's finding again, and because a bare constant's only test would write 32 MiB per run. **Three decisions, recorded because each is a choice.** Expired-but-unswept rows *count*: the ceiling protects disk and a lapsed row is on it until the hourly sweep, so excluding them would under-measure by up to a sweep interval. Under READ COMMITTED concurrent sends can overshoot by at most (pool `MaxConns` − 1) × `MaxEnvelopeBytes` — under 1 MiB, ≈3% — because each statement takes its own snapshot; closing that exactly would buy 3% for a per-recipient lock on the busiest endpoint. And **the drop is silent to both parties by construction**: the sender cannot be told, because "that queue is full" says the account exists and is not collecting its mail — a better oracle than the enumeration one §8's identical answers exist to avoid — the recipient has no channel but the full queue, and it is not logged, because a line per refused message is the per-message record §7 forbids. That silence is the accepted cost, and it is the reason the ceiling is set high enough never to bind honestly rather than merely high. **Guards:** `TestAPendingQueueStopsGrowingAtItsCeiling` (both sides of the boundary: the message landing exactly on the ceiling is stored, the next is not) and `TestAQueueAtItsCeilingIsIndistinguishableFromAnEmptyOne` (byte-for-byte on status and body), both on `verify-relay-integration.sh`'s required-name list; `TestAcknowledgingMakesRoomUnderTheCeiling` and `TestAnExpiredButUnsweptMessageStillCountsAgainstTheCeiling` support them; `config` unit tests pin that an unset variable means the default and an explicit `0` or `-1` stops startup. **Negative-tested five ways against the real files, not fixtures.** Removing the quota clause: *"the queue grew to 4128 bytes past a ceiling of 4096 — it is unbounded"*, *"the full queue holds 5120 bytes, want exactly the ceiling 4096"*, *"an expired row stopped counting before the sweep removed it"*. `<=` weakened to `<`: *"3072 bytes pending after filling the queue exactly, want 4096 — the message that lands on the ceiling was refused"*, which is one message lost per queue. Reporting the refusal to the sender as 507: *"the refused send answered 507; it must answer exactly as an accepted one does"*, **and the pre-existing `TestSendingToAStrangerIsAcceptedAndDropped` failed with it**, which is the evidence that both drops are one path. Excluding expired rows: only the row-counting test fired, which is the right blast radius for a decision. Renaming one required test: the gate exited 1 with *"1 required integration test(s) did not pass by name"* while the count floor was satisfied — 6.14's exact case. All restored and re-proved green. **Residual, and it is real:** the ceiling bounds the relay's disk, not the recipient's device. A queue at the ceiling is still ~48,000 envelopes to fetch and decrypt, and blocking is client-side and happens after decryption, so a flood is bounded but not free to receive. **`TestAcknowledgingMakesRoomUnderTheCeiling` is a supporting test with no negative control**: the defect it describes, a monotonic counter, is not expressible in an implementation that measures the rows. **Deployed to staging 2026-08-11** (`RUNBOOK-VPS.md`, the 5.39 + 5.40 deploy row), together with 5.40 and with AUDIT 6.24's compose wiring, which had been sitting undeployed. Until that happened this row described the repository and not the running box, which is the distinction 5.32 and 5.33 exist to keep. **5.40 is the same class and is deliberately not closed with it.** | CLOSED | §1.1, §1.8 | closed 2026-08-10, **deployed 2026-08-11** |
| 5.40 | **Prekey pools grew without a cumulative per-account ceiling. Closed 2026-08-11.** `PublishPreKeys` inserts with `ON CONFLICT DO NOTHING` and nothing removes a one-time prekey except dispensing it or deleting the account. `MaxPreKeysPerUpload` bounded one request and `publishLimit` bounds requests at 6/day, which left 1,200 Kyber rows/day at ≤4 KiB ≈ 5 MiB/day/account, ≈900 MiB across the 180-day `AccountRetentionDays` window. `CountOneTimePreKeys` was read only to build the publish *response*, never as a cap. The comment at `keys.go` saying storage "is otherwise unbounded by anything except the body limit" was true per request and was the sentence a reviewer would read as covering the whole question; it now says which constant does bound it. **Invariant.** Each of an account's two one-time pools holds at most `RELAY_MAX_PREKEYS_PER_ACCOUNT` rows, default **1,000** (`api.DefaultMaxPreKeysPerAccount`), counted **separately** — one shared allowance would let whichever list is stored first starve the other, and a starved Kyber pool forces every new session onto the reused last-resort key, which is 3.1's cost. **It refuses nothing honest, by the client's own arithmetic:** `MessageRepository.rotateKeysIfDue` tops up only the shortfall (`topUp = max(0, min(target - effectivePool, target))`) against a target of 100, so a rotation against a full pool publishes zero one-time keys and the server-side pool converges on 100 per kind rather than accumulating. The ceiling sits ten times above that. **The excess is clipped, not refused, and that asymmetry is the whole design.** The signed prekey and the last-resort Kyber key are written *before* the ceiling is consulted and are stored whatever the pools hold, because they are what rotation replaces (2.4) and because refusing the whole publication would be a **lockout**: the client retries a publication that never succeeded, six times a day, while every peer's session setup fails — 5.32's shape, and worse than the storage this bounds. Unlike 5.39 the drop is **not** silent and must not be: the publisher owns the data, so there is no third party to leak about, and the response already carries the resulting pool counts, which is the number the client replenishes against. A pool already over a lowered ceiling accepts nothing further and drains normally; nothing deletes the excess, because deleting a published key costs a peer the session it was about to establish. Concurrency is the same honest bound as 5.39's: the count and the inserts share a transaction but not a statement, so two racing publications can overshoot by up to one upload each, on a path capped at 6/day. **Guards:** `TestAPreKeyPoolStopsGrowingAtItsCeiling` and `TestAFullPoolStillRotatesTheLongLivedKeys`, both on `verify-relay-integration.sh`'s required-name list; `TestDispensingMakesRoomUnderThePreKeyCeiling` and `TestThePreKeyPoolsAreCountedSeparately` support them; `config` unit tests pin that unset means the default and an explicit `0` or `-1` stops startup. **Negative-tested four ways against the real files.** Removing both ceiling checks: *"after the second publication: curve 7, kyber 7, want 5 and 5 — the pools grew past their ceiling"* and three more. Refusing the whole publication when a pool is full: the anti-lockout test failed with *"publish: status 500"* while the ceiling test still passed, which is the right blast radius. Sharing one allowance between the pools: *"curve 0, kyber 3, want 3 and 3 — one pool consumed the other's allowance"*. Renaming a required test: the gate exited 1 with *"1 required integration test(s) did not pass by name"* while the count floor was satisfied. **The third negative test found a defect in this row's own test suite and is the reason it is listed:** `TestThePreKeyPoolsAreCountedSeparately` originally filled the *curve* pool and checked the Kyber one, which is not the direction the code stores first, so a deliberately shared allowance **passed** it. It now covers both directions on separate accounts. That is **R2** reproduced inside a fix for something else — a test that had never been made to fail was not testing what it claimed. | CLOSED | §1.1, §1.8 | closed 2026-08-11, **deployed the same day** |
| 5.41 | **"Mass revoke" has no non-destructive form, and the roadmap assumed it did.** Found 2026-08-11 by P9.S06 while writing the incident-response procedure that is supposed to use it. `DELETE /v1/auth/all` is **per-account and self-service** — it authenticates as the account and destroys only that caller's sessions — so an operator responding to a relay compromise has no endpoint at all. The only operator-side action is `DELETE FROM session_tokens` in psql, and its cost is not "everyone signs in again": **the session token is the only credential path.** `POST /v1/auth/rotate` needs the old token and redemption mints a *new* account, so every account whose row is deleted is unrecoverable — its owner must redeem a fresh invite, receive a different `aci`, and re-verify safety numbers with every peer. **It is also destructive on the device, on a delay**: an ordinary 401 on fetch or send is non-destructive (`Failure.unauthenticated`), but a *rotation* rejection is handled as `signOut()`, which erases local protocol state, history and profile. Rotation begins 7 days before a 30-day expiry, so each device destroys its own history whenever it next enters that window — up to 23 days after the revoke, silently, with nothing linking the two events. So the incident response the roadmap names is indistinguishable from disbanding and rebuilding the circle. **Not fixed here, deliberately**: a non-destructive mass revoke needs a re-authentication path that does not exist, and inventing one inside a documentation step is exactly what the roadmap forbids. `RUNBOOK-INCIDENT.md` documents it as a last resort with the true cost attached rather than listing it as a routine control, and the containment steps that do not need it are ordered ahead of it. **Narrowed 2026-08-13 by P9.S10, the second half only.** A rejected rotation and a lapsed credential no longer run `signOut()`: the credential takes a new persisted `sessionEnded` phase, the app stops at a screen that says the session ended, and **nothing is erased until the user confirms**. So the silent, delayed, unexplained history loss is gone. The phase is persisted rather than held in memory because it has to be a ratchet — an in-memory flag is undone by a relaunch, and for the expiry route also by winding the device clock back, which is the family of 6.23; `testForegroundExpiryPersistsTheDestructiveGate` now asserts a backwards clock cannot restore the session. The invariant the destructive path was protecting is unchanged and separately asserted: another invite stays unreachable until erasure (`testAnEndedSessionStillBlocksAnotherInvite`). **The row stays OPEN**, because the first half is untouched — the account is still unrecoverable and a mass revoke still costs every member a new `aci`. That is P9.S11. **CLOSED 2026-08-13 by P9.S11.** The invariant: an account that has published a re-authentication key can obtain a working session by signing a server-chosen, single-use challenge, so deleting `session_tokens` is a disruption rather than a disbandment. The guards, each required by name in `verify-relay-integration.sh` and each negative-tested by reintroducing the defect in the real source: `TestReauthenticationIssuesAWorkingSession` (an account with every session deleted comes back, and the token it gets actually authenticates); `TestAChallengeIsSingleUse` and `TestAWrongSignatureBurnsTheChallenge` (replay, and a failed attempt burning the challenge — removing the consume step failed both); `TestAnotherAccountsKeyCannotReauthenticate` (removing the `ed25519.Verify` failed this and one more); `TestTheAccountKeyIsWriteOnce` (dropping the write-once predicate failed it); `TestAChallengeForAnUnknownAccountLooksIdentical` (no membership oracle); and `TestOnlyThePublicHalfIsStored`, which reads the column rather than the API. A cross-language test pins the signed payload on both sides, because Swift formats a UUID uppercase and Go lowercase — a mismatch that would verify in every unit test and be refused in production. **What is accepted rather than solved (option A1):** re-authentication weakens revocation against a *stolen device*, since whoever holds the device holds the key. That attacker already holds the libsignal identity private key and can already impersonate the account, so it widens no adversary; the alternative was an operator freeze flag, a censorship lever in a design that has none. **Residual:** accounts created before migration `0003` have no key until their next authenticated launch publishes one, and cannot be given one retroactively. **CLOSED describes the repository, and the running relay does not carry it yet.** The staging box was on `6e6bbf2` when this merged — checked read-only, and the revision the runbook already records — so migration `0003` has not applied and `PUT /v1/auth/key`, `POST /v1/auth/challenge` and `POST /v1/auth/reauth` answer 404 there. Until the deploy, an operator who deletes `session_tokens` on staging still disbands the circle exactly as before. This is the distinction 5.32 and 5.33 exist to keep, and both of those cost a real device session: the deploy is an operator action, tracked as the pending `P9.S11 deploy` row in `RUNBOOK-VPS.md`. | CLOSED | §1.1, §1.3 | closed P9.S11 (repository); **staging deploy pending**; narrowed first by P9.S10 |

## 6. Platform and release

| # | Item | Status | Adversary | Fix in |
|---|---|---|---|---|
| 6.1 | **`PrivacyInfo.xcprivacy` declared only Cipher's own required-reason API usage;** libsignal ships no manifest, so its usage had to be enumerated and merged (see [`PRIVACY_MANIFEST.md`](PRIVACY_MANIFEST.md)). **The first scan read `Cipher.app/Cipher` and found nothing — a false all-clear:** libsignal links as a *dynamic framework*, so its symbols live in `Frameworks/`, are never dead-stripped, and ship whether Cipher calls them or not. `Scripts/verify-privacy-manifest.sh` now scans **every Mach-O in the bundle** and fails on any category present but undeclared. **Re-checked 2026-08-08 (P8.S05) and unchanged.** `PINS.env` still pins the exact version and commit the enumeration was performed against, so it describes the current bundle rather than a predecessor; both judgement calls were re-tested rather than restated, and the gate exits 0 against the Release build. The re-check found that **Apple restructured the documentation** — the per-category API lists moved off "Describing use of required reason API" and onto `NSPrivacyAccessedAPIType` — which is recorded in `PRIVACY_MANIFEST.md` because a later re-check reading only the old article would find no lists and could mistake that for the requirement lapsing. | CLOSED | — | P1.S11 |
| 6.2 | **libsignal's acknowledgements are not surfaced in an About screen.** AGPL obligation 3 in `NOTICE.md`. **CLOSED 2026-08-08 (P8.S07).** The obligation is a licence term, so what had to be established is that the text is *in the bundle and reachable at runtime*, not that a screen exists. CocoaPods generates the licence into `Pods/Target Support Files/`, which never ships — nothing under `Pods/` does — so `Cipher/Acknowledgements.plist` is a byte-identical copy that the app target picks up through its synchronized group, and `AboutView` renders every entry in full. **A copy drifts, so a gate holds it:** `Scripts/verify-acknowledgements.sh` (gate 12) fails when the shipped bytes stop matching the generated ones, which is what stops a pod update from leaving the previous licence on the screen, and separately refuses a plist that matches and says nothing — two empty files match perfectly. **Guards:** `AcknowledgementsTests` proves the resource ships, that libsignal's entry carries the AGPL text and more than 10 000 characters of it (a title with a fragment under it is the failure that looks like success), and that CocoaPods' header and footer are not rendered as libraries, with a positive control that the filter did not simply drop everything. Negative-tested three ways in the real files: a truncated licence, an entry CocoaPods never generated, and the resource held out of the bundle. **The first of those found a defect in the gate itself** — its positive control was the committed file, so a genuinely broken file reported that the *checker* was broken, which is **R5**'s "do not derive the cases from the data under test" reproduced inside its own fix. The control is now synthetic. | CLOSED | — | P8.S07 |
| 6.3 | **`ITSAppUsesNonExemptEncryption` is not declared.** Cipher contains encryption; the declaration and any self-classification report are legal determinations, not engineering ones. | OPEN | — | P8.S06 |
| 6.4 | **No jailbreak or debugger detection, no screenshot protection.** Hardening, not controls — none of them stops an attacker who already has code execution. | OPEN | §1.4 | P10.S04 |
| 6.5 | **The app-target manifest gate tracks only `.swift`, `.xcprivacy` and `.entitlements` under `Cipher/`.** Other resources can join the shipping bundle without a review signal, because the target uses a synchronized folder. Security-critical code does not live there — it lives in `CipherCrypto/`, which uses explicit target membership. | ACCEPTED | §1.7 | — |
| 6.6 | **The crypto test bundle is hosted by the app.** A host-less bundle has no Keychain access group, so every `SecItem` call returns `-34018` and the Keychain — where the identity key lives — would be the one component with no coverage. Bundle-level entitlements do not work around it: for simulator SDKs Xcode sets `ENTITLEMENTS_REQUIRED = NO` and silently ignores `CODE_SIGN_ENTITLEMENTS`. **Two accepted costs:** a broken app target blocks the security suite, and because the run launches an app, concurrent `xcodebuild` against one simulator fails preflight with `RequestDenied … Busy`. **`verify-all.sh` must stay serial** and retries once on preflight failure. | ACCEPTED | — | — |
| 6.7 | **A gate passed by matching its own documentation.** `verify-relay.sh` grepped the whole compose file for `--appendonly`, and the strings also appear in the comment explaining them — so deleting both flags left the gate green. The same root cause appeared inverted in `migrations_test.go`, where schema guards scanning for forbidden column names matched the migration comments that explain why those columns are absent. Both fixes are the same: **strip comments, then scan** (`migrationCode` exists for this). Six compose defects and five schema guards are now negative-tested. See **R2**. | CLOSED | §1.1 | P4.S02 |
| 6.8 | **A gate failed a correct stack by trusting a meaningless exit code.** `docker compose port postgres 5432` exits **0** with `invalid IP:0` when nothing is published, so the isolation check reported every correctly-isolated datastore as exposed. It now reads `HostConfig.PortBindings` from `docker inspect`, which is the runtime's own record and is `{}` when nothing is published. See **R2**. | CLOSED | §1.1 | P4.S03 |
| 6.9 | **Every test passed while the shipping container crash-looped.** Docker creates a named volume owned by `root:root`, and mounting one over a path the image does not contain leaves it root-owned; the relay runs as uid 65532, so it could not create its own blob directory. **The suite could not have caught it** — the integration tests run the package under a `golang` image, not the image the Dockerfile produces, so user, filesystem layout, volume ownership and read-only rootfs were entirely untested. The Dockerfile now ships an empty `--chown=65532:65532` directory at the blob path so Docker seeds the volume from it, and `verify-relay-integration.sh` builds the **real image**, waits for healthy, and requires `/health` to answer over the wire as well as through the container probe. | CLOSED | §1.1 | P4.S09 |
| 6.10 | **`THREAT_MODEL.md` §3.3 specified a control that cannot be built** — storing the APNs push token *hashed*. A push token must be replayed verbatim to Apple, so a one-way function is impossible, and the sentence read as a stronger guarantee than anything achievable. Either it would have been silently dropped, leaving the plaintext token, or "satisfied" by storing a hash beside the usable token, which is theatre. §3.3 now specifies encryption at rest under a key held only in the service environment and never in the database — **with the limit stated in the same breath: this does not defeat §1.1 host seizure**, where the environment is seized with the disk. Rotation and deletion-with-the-account remain the controls doing the real work. *(Was a second 6.6 until 2026-07-29.)* | CLOSED | §1.1, §1.5 | P4.S01 |
| 6.12 | **A new test file was added, the suite reported `TEST SUCCEEDED`, and none of its tests had run.** Only the `Cipher` app target uses a `PBXFileSystemSynchronizedRootGroup`; `CipherTests`, `CipherCrypto` and `CipherCryptoTests` use explicit membership, so a `.swift` file dropped into `CipherTests/` compiles nowhere and is silently absent from the bundle. The tally stayed at its previous value and the run was green — the same shape as R2, but arriving through the project file rather than through a check. Also note `-only-testing:CipherTests/SomeSuite` reports `Executed 0 tests` and exits 0 for a swift-testing suite addressed that way, which reads as a pass. **Rule: after adding any test file, run `bundle exec ruby Scripts/bootstrap-targets.rb`** — it globs the directory and adds the reference — **and confirm the new test's name appears in the run output.** A tally that did not change is not evidence that a test passed. | CLOSED | §1.7 | P5.S08 |
| 6.11 | **The app target compiled in Swift 5 language mode while every other target was Swift 6.** Never a decision — the Xcode template default survived every regeneration because `bootstrap-targets.rb` only patched named keys. The asymmetry was the wrong way round: the app target owns `SessionStore`, `DeviceAuthenticator` and `AppSession`'s lock state, and Swift 5 mode defaults `SWIFT_STRICT_CONCURRENCY` to `minimal`, so `Sendable` conformance is taken on trust and cross-actor access Swift 6 rejects degrades to a warning. `CipherTests` was compiling `AppLockTests` under the strictest rules against a module built under the weakest. Now set in `bootstrap-targets.rb`, because **a build setting that exists only because someone clicked it in the UI is one the next regeneration drops**. Warnings-as-errors is deliberately *not* enabled here, unlike the crypto module: this target compiles against SwiftUI, whose deprecations land on Apple's schedule, and a toolchain bump turning one into a build failure would block security work rather than enforce it. *(Was a second 6.5 until 2026-07-29.)* | CLOSED | §1.4 | P3 |
| 6.13 | **The Release build was not iPhone-only, and the project text said it was. Closed 2026-08-06.** `TARGETED_DEVICE_FAMILY = 1` excludes iPad and is what every grep found, but it is not the property: Xcode defaults `SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD` and `SUPPORTS_XR_DESIGNED_FOR_IPHONE_IPAD` to YES, and Release resolved both that way — so the shipping binary could run on an Apple silicon Mac and on visionOS. **Why that is a security finding rather than packaging:** every storage and lock decision here was argued about an iPhone — the Keychain accessibility class (2.1), the app-switcher redaction (4.5), the local-only pasteboard with its 90-second expiry (4.6), and an app lock built on device-owner authentication. None were analysed on macOS, where the container, the keychain and the window server behave differently, so the build shipped controls nobody had reasoned about in the place they would run. Both settings, plus `SUPPORTS_MACCATALYST`, are now written by `bootstrap-targets.rb` on the app target *and* at project level — the latter so a target added later inherits the restriction rather than Xcode's defaults — and set explicitly rather than left to a default, because that generator overwrites named keys and never removes them (6.11's lesson in the other direction). Guard: `Scripts/verify-iphone-only.sh`, gate 4 of `verify-all.sh`, which reads the **resolved** settings from `xcodebuild -showBuildSettings` rather than the project text — the only place the question is settled, since defaults, xcconfigs and the Pods integration all feed it and none are visible in a grep. Negative-tested twice: flipping the setting in the project fails the gate by name, and *deleting the assertion from the generator* also fails, because the stale YES then survives regeneration — which proves the generator half is load-bearing and not decoration. | CLOSED | §1.4, §1.7 | P5 build remediation |
| 6.14 | **Several existing gates could skip or under-prove their named property. Closed 2026-08-06, the last part together with 6.13.** **Pin verification (done).** It hashed the served leaf with no chain or hostname validation at all — no `-verify_return_error`, no `-verify_hostname`, and `s_client` exits 0 on a failed verification — so a self-signed certificate carrying the pinned key passed. That is the inverse of the rule `CertificatePinner` states and enforces on the client: platform validation first, the pin as an extra hurdle. Validation now runs *before* the hash and its verdict is read out of the session rather than from an exit code, because the LibreSSL macOS ships as `openssl` has already reported a false pass on this host (5.16). **Skip-green (done).** Three checks could find their material missing — no SSH to the host, no checkout, no key file — print a `!`, and let the script end `ok pin set verified` with exit 0, so a run that proved one property was indistinguishable from one that proved four. A skipped check now marks the run incomplete and exits non-zero unless the operator passes `--partial` and therefore knows what did not happen. **The backup key (done, and it found something).** `RelayEndpoint` ships two pins because one pin plus one lost key is a permanently bricked client with no server-side remedy; nothing checked that a private key matching `backupKey` still exists, so the emergency plan was a base64 string. `PIN_BACKUP_KEY_FILE` now derives the SPKI and compares it. **It has never been run against the real key** — this machine has no copy — so the gate currently reports the check as not run, which is the honest state and an operator action, not a green. **The simulator retry (done).** Its pattern list missed the observed `cannot be located on disk`, so that infrastructure fault was reported as a test failure and was not retried; four more observed messages were added with it. **Integration verification (done).** It asserted a count floor and nothing else, so a renamed or deleted *critical* test stayed green as long as the total held — demonstrated exactly that way: renaming `TestRedeemConsumesTheInvite` left the suite reporting `ok` with 125 tests above a floor of 104, and only the new by-name check caught it. Twelve tests are now required by name. **UI honesty (examined, no change needed).** The Release-bundle audit already carries three positive controls — a symbol, a Czech translation, and a probe of the email detector — which is what this row asked for. Its remaining weakness is 5.4's recorded and accepted one: a denylist stops regressions, not new inventions. **The iPhone-only property (done 2026-08-06, with 6.13).** It had no gate at all, and could not get an honest one while 6.13 was open: a gate asserting the resolved settings would have failed on the tree by design. 6.13 is now fixed and `Scripts/verify-iphone-only.sh` asserts the resolved Release settings as gate 4. Guards for what did land: `verify-pins.sh --self-test` (11 cases) and the simulator-fault classifier self-test (9 cases) both run inside `verify-all.sh`, and every control was negative-tested — deleting the observed simulator pattern names the fault it no longer recognises, inverting the “a real test failure is never retried” half fails, and each pin-validation verdict is checked against a refusing fixture. **One residual worth stating:** the pin self-test proves the parsing and the skip accounting, not a real TLS negotiation. Standing up a local server would test the flags too, and would make this gate depend on a free port, a background process and which `openssl` the machine has — a flaky gate is one that gets ignored (**R2**).  **Third pattern gap observed 2026-08-09 (audit campaign follow-up).** A run of `verify-all.sh` reported a red build for a `DerivedData` directory that disappeared mid-run: CoreSimulator emitted "Simulator device failed to install the application" and "Cannot launch simulated executable", and the list carried "Failed to install the requested application" — a different sentence from a different layer. So the classifier did not fire, the run was not retried, and an infrastructure fault was reported as a test failure with **zero** failing test cases in the log. Measured against that log rather than argued: the old expression matched it 0 times and the new one 6, with 0 `^Test Case.*failed` lines. Three literals added, each a string read from that log, and each added to the self-test the same way. This is the second time the list has been wrong in this direction, which is the whole reason its comment says a missing pattern is silent by construction. | CLOSED | §1.3, §1.7 | P5 verification remediation; the iPhone-only half with 6.13 |
| 6.15 | **The privacy manifest declares required-reason APIs but no collected-data categories, while the relay necessarily processes account identifiers, encrypted message payloads and delivery metadata.** Required-reason API disclosure and App Store privacy disclosure are different obligations. The submitted declaration must truthfully describe collection/linkage/purpose without claiming Cipher can read ciphertext content. | OPEN | §1.1, §1.5, §1.8 | P8.S06, before distribution |
| 6.16 | **Two serialisation tests measured elapsed time instead of observable state, and one of them failed in CI. Closed 2026-08-06.** `testACancelledRepositoryWaiterNeverRunsAfterTheGateOpens` failed on a runner at its *precondition* — `RoutedStubRelay.waitForBlockedRequest` allowed a fixed 2 s for the first `receive()` to reach the stub, and that call opens the crypto engine, reads the Keychain, opens SQLite and performs its first libsignal call first. The test sorts first among these, so it pays the cold start, and the bundle ran three to four times slower on CI than locally. Reproduced by shrinking the ceiling to 1 ms: byte-identical signature. **The flake was the smaller half.** Both tests then asserted after `Task.sleep` of 50 ms and 100 ms that the second call had not reached the relay — which is also true when the second call has not *started* yet, so on a slow machine the assertion held without exercising the path it names, and `waiter.cancel()` cancelled a task that had never queued. A pass for the wrong reason (**R2**), and the more dangerous direction, because it is silent. Both now wait on state the gate actually knows: `SerialGate` exposes `queuedWaiterCount` and `MessageRepository` a `nonisolated queuedOperationWaiters` over it — read-only, unread by production, and `nonisolated` because a test observing a blocked operation cannot rely on entering that actor promptly. **A third defect surfaced while fixing the first two, and only because the fix was negative-tested:** the stub self-releases a blocked response after a timeout, so a wait that outlives it stops observing “the second call is held out” and starts observing “the block expired”. That ordering had held at 5 s against 50 ms sleeps by a wide margin and by accident; raising the waits for CI inverted it, and the negative run showed the relay receiving a second request it should never have seen. The two budgets are now named constants — `blockEscapeHatch` (a deadlock escape hatch no test should reach) and `observationCeiling`, strictly below it — with the relationship written where either would be edited. Guards: both tests negative-tested by making `queuedWaiterCount` report nothing queued, where each fails by name and nothing else does, and by shrinking the wait ceiling, which reproduces the original CI failure exactly. Side effect worth stating: both tests now finish in ~0.03 s instead of sleeping out their budget. **Residual:** these remain concurrency tests with wall-clock ceilings. The ceilings now bound only a *broken* run — a healthy one never spends them — but they are still numbers, and a machine slow enough would still fail them. | CLOSED | §1.7 | CI failure on the AUDIT 1.14 branch, 2026-08-06 |
| 6.17 | **The verification gate refuses to run when it would destroy a registered account. Closed 2026-08-07.** Invariant: `Scripts/verify-all.sh` never silently wipes a simulator someone has registered and is testing against. The hazard is unchanged and cannot be removed — the crypto tests are app-hosted (6.6), so they run inside `Cipher.app` sharing its container and Keychain, and they call `destroyAllState()`. What changed is that the run now stops first. **Gate 1**, ahead of everything slow because the run this exists to stop is the one started in the background and walked away from, resolves every simulator carrying the target name and refuses when any of them holds crypto state. It ignores `--fast` and `--offline`: the app-hosted run is skipped by neither, which is why `--fast` did not avoid the original wipe. **The probe uses the store's own rule.** `container_holds_account` treats every child of `Library/Application Support/CipherCrypto` as state, deliberately matching `EncryptedFileRecordStore.containsPersistedState`, because it has to predict what `destroyAllState` would take and recognising only today's filenames is how a check concludes there is nothing to lose. **Refusing is the default, and R2 is the reason it is not only that.** A gate that cries wolf gets deleted, so the refusal fires solely on an account that exists — an empty container, a container with no `CipherCrypto` root, an absent one and an unresolvable one are each a positive control — and it names both ways forward rather than leaving commenting-it-out as the way past: `CIPHER_TEST_SIMULATOR` to run elsewhere, or `CIPHER_ALLOW_SIMULATOR_WIPE=1` to declare the account expendable. The destructive path stays deliberate and greppable instead of the quiet default. A stale `APP_BUNDLE_ID` would stop finding the container, which is indistinguishable from finding no account, so it is asserted against `project.pbxproj`; a `simctl` enumeration that returns nothing is a failure, not a pass. Guards: `selftest_container_probe`, 10 cases, run before the gate's own verdict is trusted, exactly as the simulator-fault classifier is. Negative-tested four ways in the real script — a blinded probe fails reporting it no longer sees a populated container, an always-firing probe fails on the empty-container positive control, a verdict that never refuses fails reporting it would allow the destruction, and a broken override fails reporting the override does not work, *which is how it gets removed instead*. The verdict is a pure function precisely so the branch that permits an irreversible wipe is provable without a run that actually destroyed something. Confirmed against the real hazard: the gate refused on the simulator that still held the P5.S13 account, and that account was intact afterwards. **Not a CI concern:** CI's simulator is created clean per run, so the check passes in silence there. | CLOSED | — | Simulator-safety step |
| 6.18 | **The account-destruction guard covers `verify-all.sh` and nothing else, so any direct `xcodebuild test` walks past it.** 6.17 put the refusal in **gate 1 of the script**, which is the right place for the run someone starts and walks away from — but the hazard lives in the *tests*, not in the script. The crypto tests are app-hosted (6.6), so `MessagingFixture` opens engines through `Keychain.shared` and its `tearDown` calls `destroyAllState()`, which issues `SecItemDelete` across the whole `cz.janrichtermoc.Cipher.crypto` service. A developer — or an agent — iterating with `xcodebuild test -only-testing:...` gets none of the protection. **Observed, not hypothesised:** during P6.S01 exactly that happened to the simulator holding the P5.S13 field-test installation. A read-only query of that device's keychain afterwards returned no Cipher items while `records.sqlite3` remained, which is the orphaned-local-state condition 5.34 describes. This is the shape **5.6** already names — a control applied to the buttons and not to the mechanism. The fix belongs in the fixture rather than in another script: refuse to open a real-Keychain engine when the container holds an installation, or give the tests their own Keychain service so `removeAll` cannot reach a real account. `TestSession` already does the latter for the session credential, which is why it was never at risk. Until this step the guard held only for the documented pre-commit path, and 6.17's refusal on a simulator whose keys were already gone protected a container that no longer opened. **CLOSED 2026-08-08.** The redirect is in `Keychain.shared` rather than in `MessagingFixture`, deliberately: a fixture protects the tests that already exist, and every path into this module's storage — including `CryptoEngine.open`, which is what an app-side test naturally reaches for — goes through that one handle. Under XCTest it now returns a Keychain on `cz.janrichtermoc.Cipher.crypto.xctest`; `kSecAttrService` matches exactly, so `removeAll` under it cannot reach an item stored under the production service no matter what it deletes. Detection is `NSClassFromString("XCTestCase")` **or** either XCTest environment variable, so neither signal is load bearing alone, and a developer running the app in Debug by hand matches none of them and keeps their account. Fenced out of Release: the shipped bundle was searched and contains the production service string and not the test one, the production string being the positive control that the search works (**R2**). **Guards:** `KeychainIsolationTests.testTheSharedKeychainIsNotTheProductionServiceUnderTests` pins the redirect and pins the production literal separately, so a rename that pointed one at the other fails rather than agreeing with itself; `testRemovingEverythingUnderOneServiceLeavesAnotherAlone` proves the service scoping the isolation rests on, between two test-only services. **The obvious test is deliberately absent, and this is the finding's own shape:** a canary under the production service would have to be validated by breaking the redirect, and that negative control destroys whatever real account is on the machine — a gate whose verification is the disaster it prevents (**R5**). The two properties above are each safe to prove and together give the guarantee; neither writes to the production service. Negative-tested by removing the redirect in the real source and running only the assertion-only test, which fails by name. **Residual, stated rather than implied:** this closes the *Keychain* half. A test that opened `CryptoEngine.open(container: nil)` would still delete the real container's files while writing keys to the test service — the reverse of 5.34's orphan. No test does, every one passes an explicit temporary container, and 6.17's gate-1 refusal still stands as the compensating control for the run someone starts and walks away from.| CLOSED | — | AUDIT 6.18 remediation |
| 6.19 | **Two ways a test could stop running with every gate still green. Found and closed 2026-08-09 (audit campaign).** Both are the "a successful zero-test run is a failure" rule (§0, `.claude/rules/verification.md`) applied to directions nothing checked. **(a) Explicit target membership was ungated.** `verify-app-target-manifest.sh` covers the *app* target, which uses a synchronized folder; `CipherCrypto`, `CipherCryptoTests` and `CipherTests` list every file individually, so a file dropped into one of those directories joins nothing until `bootstrap-targets.rb` is run by hand. For a source file that is loud — the first reference to it fails to compile. For a **test** file it is silent: an uncompiled `XCTestCase` produces no name, changes no total, and leaves the full verification green over a security test that never ran. **(b) The Swift Testing suites were unasserted.** Gate 14 proves `LockedDecisionsTests`, `SessionCredentialTests` and `AppLockTests` ran by name, but those three cover XCTest only. The 63 tests in `Certificate pinning`, `Invite redemption` and `Relay transport bounds` run under a different runner, are not counted by any `Executed N tests` line, and **certificate pinning has no XCTest anywhere** — the control between this client and a substituted relay (§1.3) was proved by suites nothing asserted had run. **Invariant.** Every Swift file on disk is compiled by the target that owns its directory, and every Swift Testing suite is named in gate 14. **Guards:** `Scripts/verify-target-membership.py`, run with its own `--self-test` inside gate 4, and three `grep -qF` assertions on the test log. Negative-tested against the real repository rather than fixtures: an ordinary new test file placed in `CipherTests/` failed the membership gate with "on disk, not a member of CipherTests"; and a run with `-skip-testing:CipherTests/CertificatePinnerTests` **exited 0 and printed `** TEST SUCCEEDED **`** while Swift Testing reported 50 tests in 2 suites instead of 63 in 3 — the old gate's verdict was unchanged, and the new assertion fired with "the Swift Testing suite \"Certificate pinning\" did not run". **Residual:** the suite list is three literals, so a suite added later is not covered until it is added there. Deriving it from the sources would make the check follow whatever the sources happen to contain, which is the defect **R5** records. | CLOSED | §1.3, §1.7 | audit campaign 2026-08-09 |
| 6.20 | **The relay integration gate would not have noticed four of its twelve test files disappearing. Found and closed 2026-08-09 (audit campaign).** This is **6.14** in the one place it was never applied. The floor said 104 while the suite had grown to 140, so it had drifted 36 below the truth — enough that deleting any single file still cleared it (the largest is 17 tests). And `REQUIRED_INTEGRATION_TESTS` named 22 cases that between them left four files unprotected: `adversarial` (10 tests, the only cross-account isolation checks), `blobs` (17, the only path handling anywhere), `logging` (6, which `BACKEND.md` §7 calls the only real verification of the logging rules because it runs the relay and reads what came out) and `ratelimit` (5, the token bucket's only coverage anywhere — `internal/ratelimit` has no unit tests). **Invariant.** Every integration test file has at least one case required by name, and the floor tracks the suite. **Guard:** four names added, one per unprotected file, each chosen as the case whose absence is a security regression rather than a coverage one; floor raised to 132. **Verification was partial when this row was written:** the 26 required names were each confirmed to exist in the tree and every file to be covered, but the suite itself was **not run** — Docker was unavailable on the audit machine, so `verify-relay-integration.sh` could not execute, and the guard was argued rather than observed. **Observed 2026-08-10.** The CI `relay-integration` job — whose only step is `./Scripts/verify-relay-integration.sh` — has run the gate as written, floor and required names included, and passed on three pull requests since the campaign. Local runs still cannot: Docker is unavailable on the development machine, so CI is the only place this gate executes, which is worth knowing before trusting a green local `verify-all.sh` to have covered it. | CLOSED | §1.1, §1.7 | audit campaign 2026-08-09 |
| 6.21 | **Two shipped privacy controls had no test at all. Found and closed 2026-08-09 (audit campaign).** Both are cases where the ledger or the roadmap already asserts the property, so the gap was in the evidence rather than in the behaviour. **(a) `SecurePasteboard`.** **4.6** has been CLOSED since P3.S03, and this file's legend says CLOSED means a tested control exists — nothing in either test target referenced the type. `UIPasteboard` exposes no way to read back the options an item was written with, so the difference between `SecurePasteboard.copy` and the `UIPasteboard.general.string = text` it replaces was unobservable: dropping `.localOnly` in a tidy-up reinstates Universal Clipboard forwarding of a decrypted message to every Mac and iPad on the Apple Account, with every gate green. The options are now a separate `options(now:)` — a seam, not a behaviour change. **(b) `PhotoAttachment.prepare`.** P6.S04's roadmap row claims picked photos are re-encoded through a bitmap "which drops EXIF including where the photo was taken", and that claim was reached by no test: `AttachmentTests` enters one layer below it, at `sendAttachment(bytes:)` with synthetic bytes. Encryption does not help here — the coordinates would travel *inside* the ciphertext, delivered intact to the recipient and to everyone they later forward the file to. **Guards:** `SecurePasteboardTests` (3 cases). The live `copy`-and-read-back controls for one failure only — `copy` writing nothing at all. It cannot control for the one the seam exists to catch: replacing `copy`'s body with `UIPasteboard.general.string = text` passes all three, because `UIPasteboard` has no options readback. That `copy` passes these options is established by inspection, and the test says so rather than claiming more. Corrected 2026-08-09 after review and `AttachmentTests.testPreparingAPhotoDropsItsLocationAndEXIF` plus `testAPhotoTooSmallToNeedScalingIsStillReEncoded`. **Corrected 2026-08-09 after review:** the two originally shared one 8x8 fixture, so the second asserted a subset of the first on identical input and neither reached `resized`'s scaling branch. The first now uses a 2400x1200 photo and the second an 8x8 one, so between them both paths through `resized` are covered — which is what the second test's stated purpose always was. The EXIF fixture is built in the test and asserted to *carry* GPS before the assertion that it lost it, so an encoder returning an empty file cannot pass. Negative-tested in the real source: removing `.localOnly` failed `testACopiedMessageIsMarkedLocalToThisDevice` with "without .localOnly the item is forwarded by Universal Clipboard (AUDIT 4.6)", and making `prepare` return its input unchanged failed both photo tests on the GPS dictionary and on `DateTimeOriginal`. **Noted on the way:** `CipherTests` sets no `SWIFT_DEFAULT_ACTOR_ISOLATION` while the app target sets `MainActor`, so a type needing no annotation in `Cipher/` needs `@MainActor` here — 6.11's shape, recorded rather than changed. | CLOSED | §1.4, §1.5, §1.8 | audit campaign 2026-08-09 |
| 6.22 | **The disappearing-messages disclaimer was not localizable, and shipped in English only. Found and closed 2026-08-09 (audit campaign).** `ChatInfoViews` built the footer as `Text("..." + "...")`. A concatenation is a `String` expression, so overload resolution picks `Text.init<S: StringProtocol>` rather than the `LocalizedStringKey` one: Xcode extracted no key, the catalog held none, and neither fragment appeared in it — confirmed by search, not inferred from the language rule. It was the only concatenated `Text` in the tree. `THREAT_MODEL.md` §1.8 names this exact sentence as what keeps the feature honest — disappearing messages are a courtesy against a hostile recipient, never a control, and "the setting's own footer says so" — so a Czech reader got the whole interface in Czech and this one disclaimer in English, which is **5.13** reproduced. Neither existing check could see it: check A walks catalog keys and finds orphans, so a source literal with no key is invisible, and check E only inspects keys that exist. **Invariant.** The footer is one literal, is in the catalog, and is translated wherever the interface is. **Guard:** `"cannot stop someone"` added to `WARNING_MARKERS`, which makes check E require the translation from here on. Negative-tested by removing the `cs` `stringUnit` from the real catalog: the gate failed with "is a safety warning (matches 'cannot stop someone') but has no cs translation", exit 1, restored. The comment above it also cited §1.5 (Apple) where it meant §1.8. **Residual:** this class is only detectable by a marker list, and a marker list cannot know about a warning nobody thought to mark — the same limit **5.4** records for the denylist. | CLOSED | §1.8 | audit campaign 2026-08-09 |
| 6.23 | **A user-settable clock could crash the send and receive paths. Found and closed 2026-08-09 (audit campaign).** `CryptoEngine` and `CipherProtocolStore` both defaulted their clock to `UInt64(Date().timeIntervalSince1970 * 1000)`, and `UInt64(someNegativeDouble)` traps. The iOS date picker reaches 1970 and below, so a device in a UTC-positive timezone set to its minimum produces a negative interval; `now()` is called on every `encrypt` and on every inbound `saveIdentity`, which makes it a crash on **both** message paths, reached with no attacker, no relay and no malformed input. Not a security finding by this file's own rule — no §1 adversary needs it — but recorded because it is a hard trap on the two paths that matter and because the app layer had already learned it: `MessageRepository` clamps for exactly this reason and the crypto module did not. **Invariant.** `CipherCrypto.epochMilliseconds(from:)` clamps at zero and is the only conversion in the module. Clamping rather than refusing because there is nothing to protect: the envelope timestamp is untrusted by construction and the record timestamps are display and retention hints, so none gates a cryptographic decision. **Guards:** `StoreEdgeTests.testAClockSetBeforeTheEpochClampsRatherThanTrapping` and `testAnOrdinaryClockStillConvertsToMilliseconds`, the second as the positive control — without it a function returning zero unconditionally would satisfy the first. Same change bounds `generatePublishedKeys(oneTimeCount:)` above as well as below: `2 + oneTimeCount * 2` overflows and traps before the store's own bound is applied, and the new ceiling is the relay's `MaxPreKeysPerUpload`, so a larger pool could not be published anyway. That bound is a `precondition` and therefore **not** expressible as an XCTest case; what is testable, and what would actually drift, is the default moving past the ceiling — `testTheDefaultPreKeyPoolFitsInsideTheRelaysUploadCap`. | CLOSED | — | audit campaign 2026-08-09 |
| 6.24 | **A variable the relay reads reached no container, and nothing would have noticed. Found and closed 2026-08-09 (audit campaign follow-up).** `RELAY_PUSH_TOKEN_KEY` was read by `config.Load` and passed to the store by `main`, and appeared in neither `docker-compose.yml`'s `api` environment nor `.env.example` — so setting it in `.env` had no effect at all, and the symptom would have been a refusal that reads like a code bug. It fails **closed** (no key means refuse, never store a token in the clear), so the live risk was nil and the fix was two lines. **The finding is the missing guard, not the missing line:** `verify-relay.sh`'s `check_compose` asserted published ports, `cap_drop` and Redis persistence and nothing about environment forwarding, so the identical defect recurs for the next variable — which is P8.S02's problem, since that step is where a token first gets written. **Invariant.** Every `RELAY_*` name read by `internal/config` is either forwarded to the `api` service or listed in `unforwarded_by_design` with a reason; six timeouts and size limits are on that list because their defaults are the deployment's policy. The list is written out rather than derived from what the compose file happens to contain, which would make the rule "whatever is missing is fine" (**R5**). **Guard:** one `check_compose` invariant with its own self-test case (10 → 11). Negative-tested against the real file by deleting the `RELAY_PUSH_TOKEN_KEY` line: the gate failed with "these RELAY_* variables are read by internal/config and reach no container", and the committed file's positive control fired with it. Restored. | CLOSED | §1.1 | audit campaign 2026-08-09 |
| 6.25 | **The module-boundary gate reported a violation it had never checked for. Found and closed 2026-08-11.** `verify-api-boundary.sh` read the deployment target with `SETTINGS="$(xcodebuild … -showBuildSettings 2>/dev/null)"` on its own line. Under `set -e` a failing command substitution aborts the script *at that line*, so the `[ -n "$TARGET_VERSION" ]` guard written for exactly this case was unreachable for the likeliest failure, and `2>/dev/null` threw away the only text saying what went wrong. The script exited **64 with no output at all**, and `verify-all.sh` rendered any non-zero as *"a LibSignalClient type is exposed in CipherCrypto's public API"* — an accusation about the code, from a check that never ran. **Observed, not theorised:** two simulators sharing the name `iPhone 17 Pro` made `-destination name=…` ambiguous, and a full verification run reported a boundary violation on a tree whose boundary was clean. This is **R2**'s third recorded way of going wrong ("the check aborted before running and its silence was read as an accusation") and **R5**'s bare-call trap, in a file whose own header already forbade it: *"A check that cannot run must say so; it must never look like the thing it was checking for."* The rule was written; one code path skipped it. **Invariant.** A gate that cannot run exits **78** and says why on stderr, and the caller reports that differently from a finding. Every could-not-run path in the script — the build, the missing framework, the build settings, the SDK path, the digester, an empty dump, a dump with no declarations — now goes through one `cannot_run` helper, the tool's own stderr is captured and its last lines shown instead of discarded, and the two command substitutions that could abort are assigned inside `if !`, where a failure does not trip `set -e`. **Guards:** the script's own self-test runs first and re-creates the live failure in the real script by naming a simulator that does not exist, asserting **both** that the status is 78 and that the output says *could not run* — either assertion alone passes for the wrong reason. `verify-all.sh` dispatches on 78 with a separate message. **Negative-tested five ways.** Restoring the original bare assignment with `2>/dev/null`: the self-test failed with *"an unresolvable simulator exited 64, want 78"*. Making `cannot_run` exit 78 silently: *"the gate exited 78 without saying why."* Driving the real failure path directly: exit **78**, xcodebuild's own destination list shown, where the old script gave 64 and nothing. And end to end through `verify-all.sh` with the gate stubbed — exit 78 produced *"the module-boundary gate could not run"*, exit 1 still produced *"a LibSignalClient type is exposed"*, so the dispatch discriminates rather than relabelling every failure. **Residual:** no other gate assigns a tool's output to a variable while discarding its stderr — checked across `Scripts/` — but 13 scripts run under `set -euo pipefail`, so the pattern is available to the next one written. | CLOSED | §1.7 | closed 2026-08-11 |
| 6.26 | **The certificate-pinning gate's verdict depended on the network, and twice reported an infrastructure fault as a pinning defect. Found and closed 2026-08-13.** `CertificatePinnerTests` built its `SecTrust` with `SecTrustCreateWithCertificates` and nothing else, so evaluation was judged against the *platform's* trust store: reaching a publicly trusted root from the embedded `YE1` intermediate needs material a fresh runner does not hold, and the platform went to the network for it. A fetch that stalls does not report a timeout — it returns a **rejected chain**, so `a wrong-pin server is refused even though its chain is perfectly valid` and its positive control `the same valid chain IS accepted once the correct pin is present` failed as though pinning had broken. **Observed twice in CI on changes that touched documentation only** (2026-08-13, 5.19 s and 7.26 s per test, after 25 consecutive green runs), and **never locally**, where the same evaluation completed in 14 ms from cache — the reason it was invisible on the machine the suite was written on. The fixture comment already stated the rule it broke: *a pinning test whose verdict changes with connectivity is not a test*. Embedding the intermediate had made chain **building** offline; the evaluation after it was not. **Invariant.** Every trust object this suite evaluates is judged offline, against a stated anchor, at a fixed date: `SecTrustSetAnchorCertificates` + `SecTrustSetAnchorCertificatesOnly(true)` on the embedded intermediate, `SecTrustSetNetworkFetchAllowed(false)`, and `SecTrustSetVerifyDate` inside the leaf's validity window. **Anchoring does not weaken the suite**, because the property under test is that chain validation runs before the pin and that a valid chain with the wrong key is still refused — neither is a claim about which roots Apple ships, and the live certificate stays `Scripts/verify-pins.sh`'s job. **The verify date fixes a second, scheduled failure** the first did not cause: the leaf expires 2026-10-27, and evaluation reads the clock, so both tests would have started failing that day for a reason unrelated to pinning. **Guard:** `the anchor set is what the chain is judged against, not the platform's trust store` — the same real chain against an anchor it cannot reach. Without it the anchor lines could be wrong or ignored and every other test would still pass, which is **R2**. **Negative-tested two ways in the real file.** Removing the two anchor calls: the guard failed with *"an error was expected but none was thrown"* — the platform trust store accepting the chain, which is the path that needs the network. Moving the verify date past the leaf's `notAfter`: both real-chain tests failed with exactly the CI signature, *".chainRejected"* where `.pinMismatch` and no error were expected — in 0.047 s rather than 5 to 7 seconds, which is the difference between a certificate that is expired and a fetch that never answered. Both restored and re-proved green, 14 tests. **Residual:** the suite no longer notices if Apple stops trusting Let's Encrypt's `Root YE`. That was never what it checked — it had no assertion about the root, and the outcome it produced when the platform disagreed was an unexplained pinning failure. `verify-pins.sh` compares the shipped pin with the leaf the live host serves and is the check that speaks for the real chain. | CLOSED | §1.7 | closed 2026-08-13 |
